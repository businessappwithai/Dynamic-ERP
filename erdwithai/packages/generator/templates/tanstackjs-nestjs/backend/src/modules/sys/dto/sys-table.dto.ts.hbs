/**
 * SysTable DTOs - Zod Validation Schemas
 *
 * Data Transfer Objects for sys_table entity using Zod for runtime validation.
 *
 * Generated: {{now}}
 */

import { z } from 'zod';

export const CreateSysTableSchema = z.object({
  table_name: z.string().min(1),
  name: z.string().min(1),
  description: z.string().optional(),
  entity_type: z.string().optional(),
  is_view: z.boolean().optional(),
  is_active: z.boolean().optional(),
  access_level: z.number().int().optional(),
});

export type CreateSysTableDto = z.infer<typeof CreateSysTableSchema>;

export const UpdateSysTableSchema = z.object({
  table_name: z.string().min(1).optional(),
  name: z.string().min(1).optional(),
  description: z.string().optional(),
  entity_type: z.string().optional(),
  is_view: z.boolean().optional(),
  is_active: z.boolean().optional(),
  access_level: z.number().int().optional(),
});

export type UpdateSysTableDto = z.infer<typeof UpdateSysTableSchema>;

export interface SysTableResponseDto {
  sys_table_id: string;
  table_name: string;
  name: string;
  description?: string;
  entity_type?: string;
  is_view: boolean;
  is_active: boolean;
  access_level: number;
  created_at: Date;
  updated_at: Date;
}

export interface SysTableWithColumnsResponseDto extends SysTableResponseDto {
  columns: any[];
}
