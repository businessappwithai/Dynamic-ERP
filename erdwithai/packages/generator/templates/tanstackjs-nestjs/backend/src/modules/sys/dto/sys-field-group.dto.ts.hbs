/**
 * SysFieldGroup DTOs - Zod Validation Schemas
 *
 * Data Transfer Objects for sys_field_group entity using Zod for runtime validation.
 *
 * Generated: {{now}}
 */

import { z } from 'zod';

export const CreateSysFieldGroupSchema = z.object({
  name: z.string().min(1).max(255),
  description: z.string().optional(),
  entity_name: z.string().min(1),
  seq_no: z.number().int().optional(),
  is_active: z.boolean().optional(),
});

export type CreateSysFieldGroupDto = z.infer<typeof CreateSysFieldGroupSchema>;

export const UpdateSysFieldGroupSchema = z.object({
  name: z.string().min(1).max(255).optional(),
  description: z.string().optional(),
  seq_no: z.number().int().optional(),
  is_active: z.boolean().optional(),
});

export type UpdateSysFieldGroupDto = z.infer<typeof UpdateSysFieldGroupSchema>;

export interface SysFieldGroupResponseDto {
  sys_field_group_id: string;
  name: string;
  description?: string;
  entity_name: string;
  seq_no: number;
  is_active: boolean;
  created_at: Date;
  updated_at: Date;
}

export interface SysFieldGroupWithFieldsResponseDto extends SysFieldGroupResponseDto {
  fields: any[];
}
