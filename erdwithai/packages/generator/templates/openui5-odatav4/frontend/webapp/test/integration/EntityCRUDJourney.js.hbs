/**
 * {{pascalCase entity.name}} CRUD Journey Tests
 * Generated by ERDwithAI
 *
 * Complete Create, Read, Update, Delete tests for {{entity.name}}
 */

sap.ui.define([
    "sap/ui/test/opaQunit",
    "./pages/App",
    "./pages/List",
    "./pages/Detail"
], function (opaTest) {
    "use strict";

    return {
        run: function () {
            QUnit.module("{{pascalCase entity.name}} CRUD Operations");

            // ========================================
            // CREATE Tests
            // ========================================

            opaTest("Should open create form for {{entity.name}}", function (Given, When, Then) {
                Given.iStartMyApp();

                When.onTheAppPage.iNavigateToEntity("{{entity.name}}");
                When.onTheListPage.iPressCreate();

                Then.onTheDetailPage.iShouldSeeTheDetailView();
                {{#each entity.attributes}}
                {{#unless (eq name "id")}}
                Then.onTheDetailPage.iShouldSeeEditableField("{{displayName}}");
                {{/unless}}
                {{/each}}

                Then.iTeardownMyApp();
            });

            opaTest("Should create new {{entity.name}} with valid data", function (Given, When, Then) {
                Given.iStartMyApp();

                When.onTheAppPage.iNavigateToEntity("{{entity.name}}");
                When.onTheListPage.iPressCreate();

                // Fill form fields
                {{#each entity.attributes}}
                {{#unless (eq name "id")}}
                {{#if (eq type "string")}}
                When.onTheDetailPage.iEnterValueInField("{{displayName}}", "Test {{displayName}}");
                {{else if (eq type "text")}}
                When.onTheDetailPage.iEnterTextInArea("{{displayName}}", "Test {{displayName}} content");
                {{else if (eq type "integer")}}
                When.onTheDetailPage.iEnterValueInField("{{displayName}}", "123");
                {{else if (eq type "decimal")}}
                When.onTheDetailPage.iEnterValueInField("{{displayName}}", "99.99");
                {{else if (eq type "boolean")}}
                When.onTheDetailPage.iToggleCheckbox("{{displayName}}");
                {{else if (eq type "date")}}
                When.onTheDetailPage.iSelectDate("{{displayName}}", "2024-01-15");
                {{else if (eq type "datetime")}}
                When.onTheDetailPage.iSelectDate("{{displayName}}", "2024-01-15");
                {{/if}}
                {{/unless}}
                {{/each}}

                When.onTheDetailPage.iPressSave();

                Then.onTheDetailPage.iShouldSeeSuccessMessage();
                Then.onTheListPage.iShouldSeeAtLeastOneItem();

                Then.iTeardownMyApp();
            });

            opaTest("Should validate required fields on create", function (Given, When, Then) {
                Given.iStartMyApp();

                When.onTheAppPage.iNavigateToEntity("{{entity.name}}");
                When.onTheListPage.iPressCreate();

                // Try to save without filling required fields
                When.onTheDetailPage.iPressSave();

                // Should show validation errors for required fields
                {{#each entity.attributes}}
                {{#if required}}
                {{#unless (eq name "id")}}
                Then.onTheDetailPage.iShouldSeeValidationError("{{displayName}}");
                {{/unless}}
                {{/if}}
                {{/each}}

                Then.iTeardownMyApp();
            });

            opaTest("Should cancel create and return to list", function (Given, When, Then) {
                Given.iStartMyApp();

                When.onTheAppPage.iNavigateToEntity("{{entity.name}}");
                When.onTheListPage.iPressCreate();
                When.onTheDetailPage.iPressCancel();

                Then.onTheListPage.iShouldSeeTheList();

                Then.iTeardownMyApp();
            });

            // ========================================
            // READ Tests
            // ========================================

            opaTest("Should display {{entity.name}} list", function (Given, When, Then) {
                Given.iStartMyApp();

                When.onTheAppPage.iNavigateToEntity("{{entity.name}}");

                Then.onTheListPage.iShouldSeeTheList();
                Then.onTheListPage.iShouldSeeCreateButton();

                Then.iTeardownMyApp();
            });

            opaTest("Should display {{entity.name}} details", function (Given, When, Then) {
                Given.iStartMyApp();

                When.onTheAppPage.iNavigateToEntity("{{entity.name}}");
                When.onTheListPage.iSelectItem(0);

                Then.onTheDetailPage.iShouldSeeTheDetailView();
                {{#each entity.attributes}}
                {{#unless (eq name "id")}}
                // Verify {{displayName}} field is displayed
                {{/unless}}
                {{/each}}

                Then.iTeardownMyApp();
            });

            opaTest("Should search {{plural entity.name}}", function (Given, When, Then) {
                Given.iStartMyApp();

                When.onTheAppPage.iNavigateToEntity("{{entity.name}}");
                When.onTheListPage.iSearchFor("test");

                Then.onTheListPage.iShouldSeeSearchResults("test");

                Then.iTeardownMyApp();
            });

            opaTest("Should clear search and show all {{plural entity.name}}", function (Given, When, Then) {
                Given.iStartMyApp();

                When.onTheAppPage.iNavigateToEntity("{{entity.name}}");
                When.onTheListPage.iSearchFor("test");
                When.onTheListPage.iClearSearch();

                Then.onTheListPage.iShouldSeeAtLeastOneItem();

                Then.iTeardownMyApp();
            });

            opaTest("Should refresh {{entity.name}} list", function (Given, When, Then) {
                Given.iStartMyApp();

                When.onTheAppPage.iNavigateToEntity("{{entity.name}}");
                When.onTheListPage.iPressRefresh();

                Then.onTheListPage.iShouldSeeTheList();

                Then.iTeardownMyApp();
            });

            // ========================================
            // UPDATE Tests
            // ========================================

            opaTest("Should enter edit mode for {{entity.name}}", function (Given, When, Then) {
                Given.iStartMyApp();

                When.onTheAppPage.iNavigateToEntity("{{entity.name}}");
                When.onTheListPage.iSelectItem(0);
                When.onTheDetailPage.iPressEdit();

                {{#each entity.attributes}}
                {{#unless (eq name "id")}}
                Then.onTheDetailPage.iShouldSeeEditableField("{{displayName}}");
                {{/unless}}
                {{/each}}

                Then.iTeardownMyApp();
            });

            opaTest("Should update {{entity.name}} successfully", function (Given, When, Then) {
                Given.iStartMyApp();

                When.onTheAppPage.iNavigateToEntity("{{entity.name}}");
                When.onTheListPage.iSelectItem(0);
                When.onTheDetailPage.iPressEdit();

                // Update a field
                {{#each entity.attributes}}
                {{#unless (eq name "id")}}
                {{#if (eq type "string")}}
                When.onTheDetailPage.iEnterValueInField("{{displayName}}", "Updated {{displayName}}");
                {{/if}}
                {{/unless}}
                {{/each}}

                When.onTheDetailPage.iPressSave();

                Then.onTheDetailPage.iShouldSeeSuccessMessage();

                Then.iTeardownMyApp();
            });

            opaTest("Should validate updates on {{entity.name}}", function (Given, When, Then) {
                Given.iStartMyApp();

                When.onTheAppPage.iNavigateToEntity("{{entity.name}}");
                When.onTheListPage.iSelectItem(0);
                When.onTheDetailPage.iPressEdit();

                // Clear a required field
                {{#each entity.attributes}}
                {{#if required}}
                {{#unless (eq name "id")}}
                {{#if (eq type "string")}}
                When.onTheDetailPage.iEnterValueInField("{{displayName}}", "");
                {{/if}}
                {{/unless}}
                {{/if}}
                {{/each}}

                When.onTheDetailPage.iPressSave();

                // Should show validation error
                {{#each entity.attributes}}
                {{#if required}}
                {{#unless (eq name "id")}}
                Then.onTheDetailPage.iShouldSeeValidationError("{{displayName}}");
                {{/unless}}
                {{/if}}
                {{/each}}

                Then.iTeardownMyApp();
            });

            opaTest("Should cancel update and revert changes", function (Given, When, Then) {
                Given.iStartMyApp();

                When.onTheAppPage.iNavigateToEntity("{{entity.name}}");
                When.onTheListPage.iSelectItem(0);
                When.onTheDetailPage.iPressEdit();

                // Make changes
                {{#each entity.attributes}}
                {{#unless (eq name "id")}}
                {{#if (eq type "string")}}
                When.onTheDetailPage.iEnterValueInField("{{displayName}}", "Changed {{displayName}}");
                {{/if}}
                {{/unless}}
                {{/each}}

                When.onTheDetailPage.iPressCancel();

                // Changes should be reverted
                Then.onTheDetailPage.iShouldSeeTheDetailView();

                Then.iTeardownMyApp();
            });

            // ========================================
            // DELETE Tests
            // ========================================

            opaTest("Should show delete confirmation for {{entity.name}}", function (Given, When, Then) {
                Given.iStartMyApp();

                When.onTheAppPage.iNavigateToEntity("{{entity.name}}");
                When.onTheListPage.iSelectItem(0);
                When.onTheDetailPage.iPressDelete();

                Then.onTheDetailPage.iShouldSeeDeleteConfirmation();

                Then.iTeardownMyApp();
            });

            opaTest("Should cancel delete operation", function (Given, When, Then) {
                Given.iStartMyApp();

                When.onTheAppPage.iNavigateToEntity("{{entity.name}}");
                When.onTheListPage.iSelectItem(0);
                When.onTheDetailPage.iPressDelete();
                When.onTheDetailPage.iCancelDelete();

                Then.onTheDetailPage.iShouldSeeTheDetailView();

                Then.iTeardownMyApp();
            });

            opaTest("Should delete {{entity.name}} successfully", function (Given, When, Then) {
                Given.iStartMyApp();

                When.onTheAppPage.iNavigateToEntity("{{entity.name}}");
                When.onTheListPage.iSelectItem(0);
                When.onTheDetailPage.iPressDelete();
                When.onTheDetailPage.iConfirmDelete();

                Then.onTheDetailPage.iShouldSeeSuccessMessage();
                Then.onTheListPage.iShouldSeeTheList();

                Then.iTeardownMyApp();
            });
        }
    };
});
