/**
 * SysUser DTOs - Zod Validation Schemas
 *
 * Data Transfer Objects for sys_user entity using Zod for runtime validation.
 *
 * Generated: {{now}}
 */

import { z } from 'zod';

export const CreateSysUserSchema = z.object({
  name: z.string().min(1),
  email: z.string().email(),
  password: z.string().min(8),
  description: z.string().optional(),
  is_active: z.boolean().optional(),
  is_locked: z.boolean().optional(),
});

export type CreateSysUserDto = z.infer<typeof CreateSysUserSchema>;

export const UpdateSysUserSchema = z.object({
  name: z.string().min(1).optional(),
  email: z.string().email().optional(),
  password: z.string().min(8).optional(),
  description: z.string().optional(),
  is_active: z.boolean().optional(),
  is_locked: z.boolean().optional(),
});

export type UpdateSysUserDto = z.infer<typeof UpdateSysUserSchema>;

export const ChangePasswordSchema = z.object({
  old_password: z.string().min(1),
  new_password: z.string().min(8),
});

export type ChangePasswordDto = z.infer<typeof ChangePasswordSchema>;

export interface SysUserResponseDto {
  sys_user_id: string;
  name: string;
  email: string;
  description?: string;
  is_active: boolean;
  is_locked: boolean;
  last_login?: Date;
  created_at: Date;
  updated_at: Date;
}

export interface SysUserWithRolesResponseDto extends SysUserResponseDto {
  roles: any[];
}
