/**
 * Component Tests for crm-app Frontend
 * Generated by ERDwithAI
 *
 * Tests for React components including forms and tables
 */

import { describe, it, expect, vi, beforeEach } from 'vitest';
import { screen, fireEvent, waitFor } from '@testing-library/react';
import { render, mockApiResponses, createMockFetch } from './setup';

// Import components - adjust paths as needed
// import { DynamicForm } from '../src/components/forms/dynamic-form';
// import { DynamicTable } from '../src/components/tables/dynamic-table';

describe('Component Tests', () => {
  beforeEach(() => {
    vi.resetAllMocks();
  });

  describe('Company Components', () => {
    describe('Company Form', () => {
      it('should render all form fields', async () => {
        // const { container } = render(<DynamicForm entityType="bus_company" />);

        // Example assertions - uncomment when components are available
        // expect(screen.getByLabelText(/Name/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Industry/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Website/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Phone/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Email/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Employee Count/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Annual Revenue/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Status/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Owner Id/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should validate required fields', async () => {
        // const { getByRole } = render(<DynamicForm entityType="bus_company" />);

        // Submit empty form
        // fireEvent.click(getByRole('button', { name: /submit/i }));

        // Check for validation errors
        // await waitFor(() => {
        //   expect(screen.getByText(/name is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/status is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/owner_id is required/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form submission', async () => {
        const onSubmit = vi.fn();
        // render(<DynamicForm entityType="bus_company" onSubmit={onSubmit} />);

        // Fill form fields
        // fireEvent.change(screen.getByLabelText(/Name/i), {
        //   target: { value: 'Test Name' },
        // });
        // fireEvent.change(screen.getByLabelText(/Industry/i), {
        //   target: { value: 'Test Industry' },
        // });
        // fireEvent.change(screen.getByLabelText(/Website/i), {
        //   target: { value: 'Test Website' },
        // });
        // fireEvent.change(screen.getByLabelText(/Phone/i), {
        //   target: { value: 'Test Phone' },
        // });
        // fireEvent.change(screen.getByLabelText(/Email/i), {
        //   target: { value: 'Test Email' },
        // });
        // fireEvent.change(screen.getByLabelText(/Status/i), {
        //   target: { value: 'Test Status' },
        // });
        // fireEvent.change(screen.getByLabelText(/Owner Id/i), {
        //   target: { value: 'Test Owner Id' },
        // });

        // Submit form
        // fireEvent.click(screen.getByRole('button', { name: /submit/i }));

        // Verify submission
        // await waitFor(() => {
        //   expect(onSubmit).toHaveBeenCalled();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form errors', async () => {
        // const onSubmit = vi.fn().mockRejectedValue(new Error('Submit failed'));
        // render(<DynamicForm entityType="bus_company" onSubmit={onSubmit} />);

        // Submit form with valid data
        // await waitFor(() => {
        //   expect(screen.getByText(/error/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should support edit mode', async () => {
        const existingData = mockApiResponses.company.single;
        // render(<DynamicForm entityType="bus_company" initialData={existingData} mode="edit" />);

        // Verify fields are pre-populated
        // expect(screen.getByLabelText(/Name/i)).toHaveValue(existingData.name);
        // expect(screen.getByLabelText(/Industry/i)).toHaveValue(existingData.industry);
        // expect(screen.getByLabelText(/Website/i)).toHaveValue(existingData.website);
        // expect(screen.getByLabelText(/Phone/i)).toHaveValue(existingData.phone);
        // expect(screen.getByLabelText(/Email/i)).toHaveValue(existingData.email);
        // expect(screen.getByLabelText(/Status/i)).toHaveValue(existingData.status);
        // expect(screen.getByLabelText(/Owner Id/i)).toHaveValue(existingData.owner_id);
        expect(true).toBe(true); // Placeholder
      });
    });

    describe('Company Table', () => {
      it('should render table with data', async () => {
        const data = mockApiResponses.company.list;
        // render(<DynamicTable entityType="bus_company" data={data} />);

        // Verify table headers
        // expect(screen.getByText(/Name/i)).toBeInTheDocument();
        // expect(screen.getByText(/Industry/i)).toBeInTheDocument();
        // expect(screen.getByText(/Website/i)).toBeInTheDocument();
        // expect(screen.getByText(/Phone/i)).toBeInTheDocument();
        // expect(screen.getByText(/Email/i)).toBeInTheDocument();
        // expect(screen.getByText(/Employee Count/i)).toBeInTheDocument();
        // expect(screen.getByText(/Annual Revenue/i)).toBeInTheDocument();
        // expect(screen.getByText(/Status/i)).toBeInTheDocument();
        // expect(screen.getByText(/Owner Id/i)).toBeInTheDocument();

        // Verify data rows
        // expect(screen.getAllByRole('row')).toHaveLength(data.length + 1); // +1 for header
        expect(true).toBe(true); // Placeholder
      });

      it('should support sorting', async () => {
        const data = mockApiResponses.company.list;
        // render(<DynamicTable entityType="bus_company" data={data} />);

        // Click column header to sort
        // fireEvent.click(screen.getByText(/[object Object]\.displayName/i));

        // Verify sort order
        expect(true).toBe(true); // Placeholder
      });

      it('should support row selection', async () => {
        const data = mockApiResponses.company.list;
        const onSelect = vi.fn();
        // render(<DynamicTable entityType="bus_company" data={data} onSelect={onSelect} />);

        // Click row
        // fireEvent.click(screen.getAllByRole('row')[1]);

        // Verify selection callback
        // expect(onSelect).toHaveBeenCalledWith(data[0]);
        expect(true).toBe(true); // Placeholder
      });

      it('should render empty state', async () => {
        // render(<DynamicTable entityType="bus_company" data={[]} />);

        // Verify empty message
        // expect(screen.getByText(/no data/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should support pagination', async () => {
        const manyItems = Array.from({ length: 50 }, (_, i) => ({
          ...mockApiResponses.company.single,
          id: `test-id-${i}`,
        }));
        // render(<DynamicTable entityType="bus_company" data={manyItems} pageSize={10} />);

        // Verify pagination controls
        // expect(screen.getByRole('button', { name: /next/i })).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });
    });
  });

  describe('Contact Components', () => {
    describe('Contact Form', () => {
      it('should render all form fields', async () => {
        // const { container } = render(<DynamicForm entityType="bus_contact" />);

        // Example assertions - uncomment when components are available
        // expect(screen.getByLabelText(/Company Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/First Name/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Last Name/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Email/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Phone/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Mobile/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Job Title/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Department/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Status/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Lead Source/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Owner Id/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should validate required fields', async () => {
        // const { getByRole } = render(<DynamicForm entityType="bus_contact" />);

        // Submit empty form
        // fireEvent.click(getByRole('button', { name: /submit/i }));

        // Check for validation errors
        // await waitFor(() => {
        //   expect(screen.getByText(/first_name is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/last_name is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/email is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/status is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/owner_id is required/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form submission', async () => {
        const onSubmit = vi.fn();
        // render(<DynamicForm entityType="bus_contact" onSubmit={onSubmit} />);

        // Fill form fields
        // fireEvent.change(screen.getByLabelText(/Company Id/i), {
        //   target: { value: 'Test Company Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/First Name/i), {
        //   target: { value: 'Test First Name' },
        // });
        // fireEvent.change(screen.getByLabelText(/Last Name/i), {
        //   target: { value: 'Test Last Name' },
        // });
        // fireEvent.change(screen.getByLabelText(/Email/i), {
        //   target: { value: 'Test Email' },
        // });
        // fireEvent.change(screen.getByLabelText(/Phone/i), {
        //   target: { value: 'Test Phone' },
        // });
        // fireEvent.change(screen.getByLabelText(/Mobile/i), {
        //   target: { value: 'Test Mobile' },
        // });
        // fireEvent.change(screen.getByLabelText(/Job Title/i), {
        //   target: { value: 'Test Job Title' },
        // });
        // fireEvent.change(screen.getByLabelText(/Department/i), {
        //   target: { value: 'Test Department' },
        // });
        // fireEvent.change(screen.getByLabelText(/Status/i), {
        //   target: { value: 'Test Status' },
        // });
        // fireEvent.change(screen.getByLabelText(/Lead Source/i), {
        //   target: { value: 'Test Lead Source' },
        // });
        // fireEvent.change(screen.getByLabelText(/Owner Id/i), {
        //   target: { value: 'Test Owner Id' },
        // });

        // Submit form
        // fireEvent.click(screen.getByRole('button', { name: /submit/i }));

        // Verify submission
        // await waitFor(() => {
        //   expect(onSubmit).toHaveBeenCalled();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form errors', async () => {
        // const onSubmit = vi.fn().mockRejectedValue(new Error('Submit failed'));
        // render(<DynamicForm entityType="bus_contact" onSubmit={onSubmit} />);

        // Submit form with valid data
        // await waitFor(() => {
        //   expect(screen.getByText(/error/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should support edit mode', async () => {
        const existingData = mockApiResponses.contact.single;
        // render(<DynamicForm entityType="bus_contact" initialData={existingData} mode="edit" />);

        // Verify fields are pre-populated
        // expect(screen.getByLabelText(/Company Id/i)).toHaveValue(existingData.company_id);
        // expect(screen.getByLabelText(/First Name/i)).toHaveValue(existingData.first_name);
        // expect(screen.getByLabelText(/Last Name/i)).toHaveValue(existingData.last_name);
        // expect(screen.getByLabelText(/Email/i)).toHaveValue(existingData.email);
        // expect(screen.getByLabelText(/Phone/i)).toHaveValue(existingData.phone);
        // expect(screen.getByLabelText(/Mobile/i)).toHaveValue(existingData.mobile);
        // expect(screen.getByLabelText(/Job Title/i)).toHaveValue(existingData.job_title);
        // expect(screen.getByLabelText(/Department/i)).toHaveValue(existingData.department);
        // expect(screen.getByLabelText(/Status/i)).toHaveValue(existingData.status);
        // expect(screen.getByLabelText(/Lead Source/i)).toHaveValue(existingData.lead_source);
        // expect(screen.getByLabelText(/Owner Id/i)).toHaveValue(existingData.owner_id);
        expect(true).toBe(true); // Placeholder
      });
    });

    describe('Contact Table', () => {
      it('should render table with data', async () => {
        const data = mockApiResponses.contact.list;
        // render(<DynamicTable entityType="bus_contact" data={data} />);

        // Verify table headers
        // expect(screen.getByText(/Company Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/First Name/i)).toBeInTheDocument();
        // expect(screen.getByText(/Last Name/i)).toBeInTheDocument();
        // expect(screen.getByText(/Email/i)).toBeInTheDocument();
        // expect(screen.getByText(/Phone/i)).toBeInTheDocument();
        // expect(screen.getByText(/Mobile/i)).toBeInTheDocument();
        // expect(screen.getByText(/Job Title/i)).toBeInTheDocument();
        // expect(screen.getByText(/Department/i)).toBeInTheDocument();
        // expect(screen.getByText(/Status/i)).toBeInTheDocument();
        // expect(screen.getByText(/Lead Source/i)).toBeInTheDocument();
        // expect(screen.getByText(/Owner Id/i)).toBeInTheDocument();

        // Verify data rows
        // expect(screen.getAllByRole('row')).toHaveLength(data.length + 1); // +1 for header
        expect(true).toBe(true); // Placeholder
      });

      it('should support sorting', async () => {
        const data = mockApiResponses.contact.list;
        // render(<DynamicTable entityType="bus_contact" data={data} />);

        // Click column header to sort
        // fireEvent.click(screen.getByText(/[object Object]\.displayName/i));

        // Verify sort order
        expect(true).toBe(true); // Placeholder
      });

      it('should support row selection', async () => {
        const data = mockApiResponses.contact.list;
        const onSelect = vi.fn();
        // render(<DynamicTable entityType="bus_contact" data={data} onSelect={onSelect} />);

        // Click row
        // fireEvent.click(screen.getAllByRole('row')[1]);

        // Verify selection callback
        // expect(onSelect).toHaveBeenCalledWith(data[0]);
        expect(true).toBe(true); // Placeholder
      });

      it('should render empty state', async () => {
        // render(<DynamicTable entityType="bus_contact" data={[]} />);

        // Verify empty message
        // expect(screen.getByText(/no data/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should support pagination', async () => {
        const manyItems = Array.from({ length: 50 }, (_, i) => ({
          ...mockApiResponses.contact.single,
          id: `test-id-${i}`,
        }));
        // render(<DynamicTable entityType="bus_contact" data={manyItems} pageSize={10} />);

        // Verify pagination controls
        // expect(screen.getByRole('button', { name: /next/i })).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });
    });
  });

  describe('Deal Components', () => {
    describe('Deal Form', () => {
      it('should render all form fields', async () => {
        // const { container } = render(<DynamicForm entityType="bus_deal" />);

        // Example assertions - uncomment when components are available
        // expect(screen.getByLabelText(/Company Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Contact Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Name/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Amount/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Currency/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Stage/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Probability/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Expected Close Date/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Actual Close Date/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Status/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Description/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Owner Id/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should validate required fields', async () => {
        // const { getByRole } = render(<DynamicForm entityType="bus_deal" />);

        // Submit empty form
        // fireEvent.click(getByRole('button', { name: /submit/i }));

        // Check for validation errors
        // await waitFor(() => {
        //   expect(screen.getByText(/name is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/currency is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/stage is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/status is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/owner_id is required/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form submission', async () => {
        const onSubmit = vi.fn();
        // render(<DynamicForm entityType="bus_deal" onSubmit={onSubmit} />);

        // Fill form fields
        // fireEvent.change(screen.getByLabelText(/Company Id/i), {
        //   target: { value: 'Test Company Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/Contact Id/i), {
        //   target: { value: 'Test Contact Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/Name/i), {
        //   target: { value: 'Test Name' },
        // });
        // fireEvent.change(screen.getByLabelText(/Currency/i), {
        //   target: { value: 'Test Currency' },
        // });
        // fireEvent.change(screen.getByLabelText(/Stage/i), {
        //   target: { value: 'Test Stage' },
        // });
        // fireEvent.change(screen.getByLabelText(/Status/i), {
        //   target: { value: 'Test Status' },
        // });
        // fireEvent.change(screen.getByLabelText(/Owner Id/i), {
        //   target: { value: 'Test Owner Id' },
        // });

        // Submit form
        // fireEvent.click(screen.getByRole('button', { name: /submit/i }));

        // Verify submission
        // await waitFor(() => {
        //   expect(onSubmit).toHaveBeenCalled();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form errors', async () => {
        // const onSubmit = vi.fn().mockRejectedValue(new Error('Submit failed'));
        // render(<DynamicForm entityType="bus_deal" onSubmit={onSubmit} />);

        // Submit form with valid data
        // await waitFor(() => {
        //   expect(screen.getByText(/error/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should support edit mode', async () => {
        const existingData = mockApiResponses.deal.single;
        // render(<DynamicForm entityType="bus_deal" initialData={existingData} mode="edit" />);

        // Verify fields are pre-populated
        // expect(screen.getByLabelText(/Company Id/i)).toHaveValue(existingData.company_id);
        // expect(screen.getByLabelText(/Contact Id/i)).toHaveValue(existingData.contact_id);
        // expect(screen.getByLabelText(/Name/i)).toHaveValue(existingData.name);
        // expect(screen.getByLabelText(/Currency/i)).toHaveValue(existingData.currency);
        // expect(screen.getByLabelText(/Stage/i)).toHaveValue(existingData.stage);
        // expect(screen.getByLabelText(/Status/i)).toHaveValue(existingData.status);
        // expect(screen.getByLabelText(/Owner Id/i)).toHaveValue(existingData.owner_id);
        expect(true).toBe(true); // Placeholder
      });
    });

    describe('Deal Table', () => {
      it('should render table with data', async () => {
        const data = mockApiResponses.deal.list;
        // render(<DynamicTable entityType="bus_deal" data={data} />);

        // Verify table headers
        // expect(screen.getByText(/Company Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Contact Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Name/i)).toBeInTheDocument();
        // expect(screen.getByText(/Amount/i)).toBeInTheDocument();
        // expect(screen.getByText(/Currency/i)).toBeInTheDocument();
        // expect(screen.getByText(/Stage/i)).toBeInTheDocument();
        // expect(screen.getByText(/Probability/i)).toBeInTheDocument();
        // expect(screen.getByText(/Expected Close Date/i)).toBeInTheDocument();
        // expect(screen.getByText(/Actual Close Date/i)).toBeInTheDocument();
        // expect(screen.getByText(/Status/i)).toBeInTheDocument();
        // expect(screen.getByText(/Description/i)).toBeInTheDocument();
        // expect(screen.getByText(/Owner Id/i)).toBeInTheDocument();

        // Verify data rows
        // expect(screen.getAllByRole('row')).toHaveLength(data.length + 1); // +1 for header
        expect(true).toBe(true); // Placeholder
      });

      it('should support sorting', async () => {
        const data = mockApiResponses.deal.list;
        // render(<DynamicTable entityType="bus_deal" data={data} />);

        // Click column header to sort
        // fireEvent.click(screen.getByText(/[object Object]\.displayName/i));

        // Verify sort order
        expect(true).toBe(true); // Placeholder
      });

      it('should support row selection', async () => {
        const data = mockApiResponses.deal.list;
        const onSelect = vi.fn();
        // render(<DynamicTable entityType="bus_deal" data={data} onSelect={onSelect} />);

        // Click row
        // fireEvent.click(screen.getAllByRole('row')[1]);

        // Verify selection callback
        // expect(onSelect).toHaveBeenCalledWith(data[0]);
        expect(true).toBe(true); // Placeholder
      });

      it('should render empty state', async () => {
        // render(<DynamicTable entityType="bus_deal" data={[]} />);

        // Verify empty message
        // expect(screen.getByText(/no data/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should support pagination', async () => {
        const manyItems = Array.from({ length: 50 }, (_, i) => ({
          ...mockApiResponses.deal.single,
          id: `test-id-${i}`,
        }));
        // render(<DynamicTable entityType="bus_deal" data={manyItems} pageSize={10} />);

        // Verify pagination controls
        // expect(screen.getByRole('button', { name: /next/i })).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });
    });
  });

  describe('DealStage Components', () => {
    describe('DealStage Form', () => {
      it('should render all form fields', async () => {
        // const { container } = render(<DynamicForm entityType="bus_deal_stage" />);

        // Example assertions - uncomment when components are available
        // expect(screen.getByLabelText(/Pipeline Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Name/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Sort Order/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Default Probability/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Is Won/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Is Lost/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should validate required fields', async () => {
        // const { getByRole } = render(<DynamicForm entityType="bus_deal_stage" />);

        // Submit empty form
        // fireEvent.click(getByRole('button', { name: /submit/i }));

        // Check for validation errors
        // await waitFor(() => {
        //   expect(screen.getByText(/pipeline_id is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/name is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/sort_order is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/default_probability is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/is_won is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/is_lost is required/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form submission', async () => {
        const onSubmit = vi.fn();
        // render(<DynamicForm entityType="bus_deal_stage" onSubmit={onSubmit} />);

        // Fill form fields
        // fireEvent.change(screen.getByLabelText(/Pipeline Id/i), {
        //   target: { value: 'Test Pipeline Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/Name/i), {
        //   target: { value: 'Test Name' },
        // });

        // Submit form
        // fireEvent.click(screen.getByRole('button', { name: /submit/i }));

        // Verify submission
        // await waitFor(() => {
        //   expect(onSubmit).toHaveBeenCalled();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form errors', async () => {
        // const onSubmit = vi.fn().mockRejectedValue(new Error('Submit failed'));
        // render(<DynamicForm entityType="bus_deal_stage" onSubmit={onSubmit} />);

        // Submit form with valid data
        // await waitFor(() => {
        //   expect(screen.getByText(/error/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should support edit mode', async () => {
        const existingData = mockApiResponses.dealStage.single;
        // render(<DynamicForm entityType="bus_deal_stage" initialData={existingData} mode="edit" />);

        // Verify fields are pre-populated
        // expect(screen.getByLabelText(/Pipeline Id/i)).toHaveValue(existingData.pipeline_id);
        // expect(screen.getByLabelText(/Name/i)).toHaveValue(existingData.name);
        expect(true).toBe(true); // Placeholder
      });
    });

    describe('DealStage Table', () => {
      it('should render table with data', async () => {
        const data = mockApiResponses.dealStage.list;
        // render(<DynamicTable entityType="bus_deal_stage" data={data} />);

        // Verify table headers
        // expect(screen.getByText(/Pipeline Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Name/i)).toBeInTheDocument();
        // expect(screen.getByText(/Sort Order/i)).toBeInTheDocument();
        // expect(screen.getByText(/Default Probability/i)).toBeInTheDocument();
        // expect(screen.getByText(/Is Won/i)).toBeInTheDocument();
        // expect(screen.getByText(/Is Lost/i)).toBeInTheDocument();

        // Verify data rows
        // expect(screen.getAllByRole('row')).toHaveLength(data.length + 1); // +1 for header
        expect(true).toBe(true); // Placeholder
      });

      it('should support sorting', async () => {
        const data = mockApiResponses.dealStage.list;
        // render(<DynamicTable entityType="bus_deal_stage" data={data} />);

        // Click column header to sort
        // fireEvent.click(screen.getByText(/[object Object]\.displayName/i));

        // Verify sort order
        expect(true).toBe(true); // Placeholder
      });

      it('should support row selection', async () => {
        const data = mockApiResponses.dealStage.list;
        const onSelect = vi.fn();
        // render(<DynamicTable entityType="bus_deal_stage" data={data} onSelect={onSelect} />);

        // Click row
        // fireEvent.click(screen.getAllByRole('row')[1]);

        // Verify selection callback
        // expect(onSelect).toHaveBeenCalledWith(data[0]);
        expect(true).toBe(true); // Placeholder
      });

      it('should render empty state', async () => {
        // render(<DynamicTable entityType="bus_deal_stage" data={[]} />);

        // Verify empty message
        // expect(screen.getByText(/no data/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should support pagination', async () => {
        const manyItems = Array.from({ length: 50 }, (_, i) => ({
          ...mockApiResponses.dealStage.single,
          id: `test-id-${i}`,
        }));
        // render(<DynamicTable entityType="bus_deal_stage" data={manyItems} pageSize={10} />);

        // Verify pagination controls
        // expect(screen.getByRole('button', { name: /next/i })).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });
    });
  });

  describe('Pipeline Components', () => {
    describe('Pipeline Form', () => {
      it('should render all form fields', async () => {
        // const { container } = render(<DynamicForm entityType="bus_pipeline" />);

        // Example assertions - uncomment when components are available
        // expect(screen.getByLabelText(/Name/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Is Default/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Is Active/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should validate required fields', async () => {
        // const { getByRole } = render(<DynamicForm entityType="bus_pipeline" />);

        // Submit empty form
        // fireEvent.click(getByRole('button', { name: /submit/i }));

        // Check for validation errors
        // await waitFor(() => {
        //   expect(screen.getByText(/name is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/is_default is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/is_active is required/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form submission', async () => {
        const onSubmit = vi.fn();
        // render(<DynamicForm entityType="bus_pipeline" onSubmit={onSubmit} />);

        // Fill form fields
        // fireEvent.change(screen.getByLabelText(/Name/i), {
        //   target: { value: 'Test Name' },
        // });

        // Submit form
        // fireEvent.click(screen.getByRole('button', { name: /submit/i }));

        // Verify submission
        // await waitFor(() => {
        //   expect(onSubmit).toHaveBeenCalled();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form errors', async () => {
        // const onSubmit = vi.fn().mockRejectedValue(new Error('Submit failed'));
        // render(<DynamicForm entityType="bus_pipeline" onSubmit={onSubmit} />);

        // Submit form with valid data
        // await waitFor(() => {
        //   expect(screen.getByText(/error/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should support edit mode', async () => {
        const existingData = mockApiResponses.pipeline.single;
        // render(<DynamicForm entityType="bus_pipeline" initialData={existingData} mode="edit" />);

        // Verify fields are pre-populated
        // expect(screen.getByLabelText(/Name/i)).toHaveValue(existingData.name);
        expect(true).toBe(true); // Placeholder
      });
    });

    describe('Pipeline Table', () => {
      it('should render table with data', async () => {
        const data = mockApiResponses.pipeline.list;
        // render(<DynamicTable entityType="bus_pipeline" data={data} />);

        // Verify table headers
        // expect(screen.getByText(/Name/i)).toBeInTheDocument();
        // expect(screen.getByText(/Is Default/i)).toBeInTheDocument();
        // expect(screen.getByText(/Is Active/i)).toBeInTheDocument();

        // Verify data rows
        // expect(screen.getAllByRole('row')).toHaveLength(data.length + 1); // +1 for header
        expect(true).toBe(true); // Placeholder
      });

      it('should support sorting', async () => {
        const data = mockApiResponses.pipeline.list;
        // render(<DynamicTable entityType="bus_pipeline" data={data} />);

        // Click column header to sort
        // fireEvent.click(screen.getByText(/[object Object]\.displayName/i));

        // Verify sort order
        expect(true).toBe(true); // Placeholder
      });

      it('should support row selection', async () => {
        const data = mockApiResponses.pipeline.list;
        const onSelect = vi.fn();
        // render(<DynamicTable entityType="bus_pipeline" data={data} onSelect={onSelect} />);

        // Click row
        // fireEvent.click(screen.getAllByRole('row')[1]);

        // Verify selection callback
        // expect(onSelect).toHaveBeenCalledWith(data[0]);
        expect(true).toBe(true); // Placeholder
      });

      it('should render empty state', async () => {
        // render(<DynamicTable entityType="bus_pipeline" data={[]} />);

        // Verify empty message
        // expect(screen.getByText(/no data/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should support pagination', async () => {
        const manyItems = Array.from({ length: 50 }, (_, i) => ({
          ...mockApiResponses.pipeline.single,
          id: `test-id-${i}`,
        }));
        // render(<DynamicTable entityType="bus_pipeline" data={manyItems} pageSize={10} />);

        // Verify pagination controls
        // expect(screen.getByRole('button', { name: /next/i })).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });
    });
  });

  describe('Activity Components', () => {
    describe('Activity Form', () => {
      it('should render all form fields', async () => {
        // const { container } = render(<DynamicForm entityType="bus_activity" />);

        // Example assertions - uncomment when components are available
        // expect(screen.getByLabelText(/Contact Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Company Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Deal Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Activity Type/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Subject/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Description/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Scheduled At/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Completed At/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Duration Minutes/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Status/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Owner Id/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should validate required fields', async () => {
        // const { getByRole } = render(<DynamicForm entityType="bus_activity" />);

        // Submit empty form
        // fireEvent.click(getByRole('button', { name: /submit/i }));

        // Check for validation errors
        // await waitFor(() => {
        //   expect(screen.getByText(/activity_type is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/subject is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/status is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/owner_id is required/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form submission', async () => {
        const onSubmit = vi.fn();
        // render(<DynamicForm entityType="bus_activity" onSubmit={onSubmit} />);

        // Fill form fields
        // fireEvent.change(screen.getByLabelText(/Contact Id/i), {
        //   target: { value: 'Test Contact Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/Company Id/i), {
        //   target: { value: 'Test Company Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/Deal Id/i), {
        //   target: { value: 'Test Deal Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/Activity Type/i), {
        //   target: { value: 'Test Activity Type' },
        // });
        // fireEvent.change(screen.getByLabelText(/Subject/i), {
        //   target: { value: 'Test Subject' },
        // });
        // fireEvent.change(screen.getByLabelText(/Status/i), {
        //   target: { value: 'Test Status' },
        // });
        // fireEvent.change(screen.getByLabelText(/Owner Id/i), {
        //   target: { value: 'Test Owner Id' },
        // });

        // Submit form
        // fireEvent.click(screen.getByRole('button', { name: /submit/i }));

        // Verify submission
        // await waitFor(() => {
        //   expect(onSubmit).toHaveBeenCalled();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form errors', async () => {
        // const onSubmit = vi.fn().mockRejectedValue(new Error('Submit failed'));
        // render(<DynamicForm entityType="bus_activity" onSubmit={onSubmit} />);

        // Submit form with valid data
        // await waitFor(() => {
        //   expect(screen.getByText(/error/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should support edit mode', async () => {
        const existingData = mockApiResponses.activity.single;
        // render(<DynamicForm entityType="bus_activity" initialData={existingData} mode="edit" />);

        // Verify fields are pre-populated
        // expect(screen.getByLabelText(/Contact Id/i)).toHaveValue(existingData.contact_id);
        // expect(screen.getByLabelText(/Company Id/i)).toHaveValue(existingData.company_id);
        // expect(screen.getByLabelText(/Deal Id/i)).toHaveValue(existingData.deal_id);
        // expect(screen.getByLabelText(/Activity Type/i)).toHaveValue(existingData.activity_type);
        // expect(screen.getByLabelText(/Subject/i)).toHaveValue(existingData.subject);
        // expect(screen.getByLabelText(/Status/i)).toHaveValue(existingData.status);
        // expect(screen.getByLabelText(/Owner Id/i)).toHaveValue(existingData.owner_id);
        expect(true).toBe(true); // Placeholder
      });
    });

    describe('Activity Table', () => {
      it('should render table with data', async () => {
        const data = mockApiResponses.activity.list;
        // render(<DynamicTable entityType="bus_activity" data={data} />);

        // Verify table headers
        // expect(screen.getByText(/Contact Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Company Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Deal Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Activity Type/i)).toBeInTheDocument();
        // expect(screen.getByText(/Subject/i)).toBeInTheDocument();
        // expect(screen.getByText(/Description/i)).toBeInTheDocument();
        // expect(screen.getByText(/Scheduled At/i)).toBeInTheDocument();
        // expect(screen.getByText(/Completed At/i)).toBeInTheDocument();
        // expect(screen.getByText(/Duration Minutes/i)).toBeInTheDocument();
        // expect(screen.getByText(/Status/i)).toBeInTheDocument();
        // expect(screen.getByText(/Owner Id/i)).toBeInTheDocument();

        // Verify data rows
        // expect(screen.getAllByRole('row')).toHaveLength(data.length + 1); // +1 for header
        expect(true).toBe(true); // Placeholder
      });

      it('should support sorting', async () => {
        const data = mockApiResponses.activity.list;
        // render(<DynamicTable entityType="bus_activity" data={data} />);

        // Click column header to sort
        // fireEvent.click(screen.getByText(/[object Object]\.displayName/i));

        // Verify sort order
        expect(true).toBe(true); // Placeholder
      });

      it('should support row selection', async () => {
        const data = mockApiResponses.activity.list;
        const onSelect = vi.fn();
        // render(<DynamicTable entityType="bus_activity" data={data} onSelect={onSelect} />);

        // Click row
        // fireEvent.click(screen.getAllByRole('row')[1]);

        // Verify selection callback
        // expect(onSelect).toHaveBeenCalledWith(data[0]);
        expect(true).toBe(true); // Placeholder
      });

      it('should render empty state', async () => {
        // render(<DynamicTable entityType="bus_activity" data={[]} />);

        // Verify empty message
        // expect(screen.getByText(/no data/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should support pagination', async () => {
        const manyItems = Array.from({ length: 50 }, (_, i) => ({
          ...mockApiResponses.activity.single,
          id: `test-id-${i}`,
        }));
        // render(<DynamicTable entityType="bus_activity" data={manyItems} pageSize={10} />);

        // Verify pagination controls
        // expect(screen.getByRole('button', { name: /next/i })).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });
    });
  });

  describe('Note Components', () => {
    describe('Note Form', () => {
      it('should render all form fields', async () => {
        // const { container } = render(<DynamicForm entityType="bus_note" />);

        // Example assertions - uncomment when components are available
        // expect(screen.getByLabelText(/Contact Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Company Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Deal Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Content/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Is Pinned/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Author Id/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should validate required fields', async () => {
        // const { getByRole } = render(<DynamicForm entityType="bus_note" />);

        // Submit empty form
        // fireEvent.click(getByRole('button', { name: /submit/i }));

        // Check for validation errors
        // await waitFor(() => {
        //   expect(screen.getByText(/content is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/is_pinned is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/author_id is required/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form submission', async () => {
        const onSubmit = vi.fn();
        // render(<DynamicForm entityType="bus_note" onSubmit={onSubmit} />);

        // Fill form fields
        // fireEvent.change(screen.getByLabelText(/Contact Id/i), {
        //   target: { value: 'Test Contact Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/Company Id/i), {
        //   target: { value: 'Test Company Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/Deal Id/i), {
        //   target: { value: 'Test Deal Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/Author Id/i), {
        //   target: { value: 'Test Author Id' },
        // });

        // Submit form
        // fireEvent.click(screen.getByRole('button', { name: /submit/i }));

        // Verify submission
        // await waitFor(() => {
        //   expect(onSubmit).toHaveBeenCalled();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form errors', async () => {
        // const onSubmit = vi.fn().mockRejectedValue(new Error('Submit failed'));
        // render(<DynamicForm entityType="bus_note" onSubmit={onSubmit} />);

        // Submit form with valid data
        // await waitFor(() => {
        //   expect(screen.getByText(/error/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should support edit mode', async () => {
        const existingData = mockApiResponses.note.single;
        // render(<DynamicForm entityType="bus_note" initialData={existingData} mode="edit" />);

        // Verify fields are pre-populated
        // expect(screen.getByLabelText(/Contact Id/i)).toHaveValue(existingData.contact_id);
        // expect(screen.getByLabelText(/Company Id/i)).toHaveValue(existingData.company_id);
        // expect(screen.getByLabelText(/Deal Id/i)).toHaveValue(existingData.deal_id);
        // expect(screen.getByLabelText(/Author Id/i)).toHaveValue(existingData.author_id);
        expect(true).toBe(true); // Placeholder
      });
    });

    describe('Note Table', () => {
      it('should render table with data', async () => {
        const data = mockApiResponses.note.list;
        // render(<DynamicTable entityType="bus_note" data={data} />);

        // Verify table headers
        // expect(screen.getByText(/Contact Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Company Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Deal Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Content/i)).toBeInTheDocument();
        // expect(screen.getByText(/Is Pinned/i)).toBeInTheDocument();
        // expect(screen.getByText(/Author Id/i)).toBeInTheDocument();

        // Verify data rows
        // expect(screen.getAllByRole('row')).toHaveLength(data.length + 1); // +1 for header
        expect(true).toBe(true); // Placeholder
      });

      it('should support sorting', async () => {
        const data = mockApiResponses.note.list;
        // render(<DynamicTable entityType="bus_note" data={data} />);

        // Click column header to sort
        // fireEvent.click(screen.getByText(/[object Object]\.displayName/i));

        // Verify sort order
        expect(true).toBe(true); // Placeholder
      });

      it('should support row selection', async () => {
        const data = mockApiResponses.note.list;
        const onSelect = vi.fn();
        // render(<DynamicTable entityType="bus_note" data={data} onSelect={onSelect} />);

        // Click row
        // fireEvent.click(screen.getAllByRole('row')[1]);

        // Verify selection callback
        // expect(onSelect).toHaveBeenCalledWith(data[0]);
        expect(true).toBe(true); // Placeholder
      });

      it('should render empty state', async () => {
        // render(<DynamicTable entityType="bus_note" data={[]} />);

        // Verify empty message
        // expect(screen.getByText(/no data/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should support pagination', async () => {
        const manyItems = Array.from({ length: 50 }, (_, i) => ({
          ...mockApiResponses.note.single,
          id: `test-id-${i}`,
        }));
        // render(<DynamicTable entityType="bus_note" data={manyItems} pageSize={10} />);

        // Verify pagination controls
        // expect(screen.getByRole('button', { name: /next/i })).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });
    });
  });

  describe('Task Components', () => {
    describe('Task Form', () => {
      it('should render all form fields', async () => {
        // const { container } = render(<DynamicForm entityType="bus_task" />);

        // Example assertions - uncomment when components are available
        // expect(screen.getByLabelText(/Contact Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Company Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Deal Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Title/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Description/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Priority/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Status/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Due Date/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Completed At/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Assigned To/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Created By/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should validate required fields', async () => {
        // const { getByRole } = render(<DynamicForm entityType="bus_task" />);

        // Submit empty form
        // fireEvent.click(getByRole('button', { name: /submit/i }));

        // Check for validation errors
        // await waitFor(() => {
        //   expect(screen.getByText(/title is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/priority is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/status is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/assigned_to is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/created_by is required/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form submission', async () => {
        const onSubmit = vi.fn();
        // render(<DynamicForm entityType="bus_task" onSubmit={onSubmit} />);

        // Fill form fields
        // fireEvent.change(screen.getByLabelText(/Contact Id/i), {
        //   target: { value: 'Test Contact Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/Company Id/i), {
        //   target: { value: 'Test Company Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/Deal Id/i), {
        //   target: { value: 'Test Deal Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/Title/i), {
        //   target: { value: 'Test Title' },
        // });
        // fireEvent.change(screen.getByLabelText(/Priority/i), {
        //   target: { value: 'Test Priority' },
        // });
        // fireEvent.change(screen.getByLabelText(/Status/i), {
        //   target: { value: 'Test Status' },
        // });
        // fireEvent.change(screen.getByLabelText(/Assigned To/i), {
        //   target: { value: 'Test Assigned To' },
        // });
        // fireEvent.change(screen.getByLabelText(/Created By/i), {
        //   target: { value: 'Test Created By' },
        // });

        // Submit form
        // fireEvent.click(screen.getByRole('button', { name: /submit/i }));

        // Verify submission
        // await waitFor(() => {
        //   expect(onSubmit).toHaveBeenCalled();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form errors', async () => {
        // const onSubmit = vi.fn().mockRejectedValue(new Error('Submit failed'));
        // render(<DynamicForm entityType="bus_task" onSubmit={onSubmit} />);

        // Submit form with valid data
        // await waitFor(() => {
        //   expect(screen.getByText(/error/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should support edit mode', async () => {
        const existingData = mockApiResponses.task.single;
        // render(<DynamicForm entityType="bus_task" initialData={existingData} mode="edit" />);

        // Verify fields are pre-populated
        // expect(screen.getByLabelText(/Contact Id/i)).toHaveValue(existingData.contact_id);
        // expect(screen.getByLabelText(/Company Id/i)).toHaveValue(existingData.company_id);
        // expect(screen.getByLabelText(/Deal Id/i)).toHaveValue(existingData.deal_id);
        // expect(screen.getByLabelText(/Title/i)).toHaveValue(existingData.title);
        // expect(screen.getByLabelText(/Priority/i)).toHaveValue(existingData.priority);
        // expect(screen.getByLabelText(/Status/i)).toHaveValue(existingData.status);
        // expect(screen.getByLabelText(/Assigned To/i)).toHaveValue(existingData.assigned_to);
        // expect(screen.getByLabelText(/Created By/i)).toHaveValue(existingData.created_by);
        expect(true).toBe(true); // Placeholder
      });
    });

    describe('Task Table', () => {
      it('should render table with data', async () => {
        const data = mockApiResponses.task.list;
        // render(<DynamicTable entityType="bus_task" data={data} />);

        // Verify table headers
        // expect(screen.getByText(/Contact Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Company Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Deal Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Title/i)).toBeInTheDocument();
        // expect(screen.getByText(/Description/i)).toBeInTheDocument();
        // expect(screen.getByText(/Priority/i)).toBeInTheDocument();
        // expect(screen.getByText(/Status/i)).toBeInTheDocument();
        // expect(screen.getByText(/Due Date/i)).toBeInTheDocument();
        // expect(screen.getByText(/Completed At/i)).toBeInTheDocument();
        // expect(screen.getByText(/Assigned To/i)).toBeInTheDocument();
        // expect(screen.getByText(/Created By/i)).toBeInTheDocument();

        // Verify data rows
        // expect(screen.getAllByRole('row')).toHaveLength(data.length + 1); // +1 for header
        expect(true).toBe(true); // Placeholder
      });

      it('should support sorting', async () => {
        const data = mockApiResponses.task.list;
        // render(<DynamicTable entityType="bus_task" data={data} />);

        // Click column header to sort
        // fireEvent.click(screen.getByText(/[object Object]\.displayName/i));

        // Verify sort order
        expect(true).toBe(true); // Placeholder
      });

      it('should support row selection', async () => {
        const data = mockApiResponses.task.list;
        const onSelect = vi.fn();
        // render(<DynamicTable entityType="bus_task" data={data} onSelect={onSelect} />);

        // Click row
        // fireEvent.click(screen.getAllByRole('row')[1]);

        // Verify selection callback
        // expect(onSelect).toHaveBeenCalledWith(data[0]);
        expect(true).toBe(true); // Placeholder
      });

      it('should render empty state', async () => {
        // render(<DynamicTable entityType="bus_task" data={[]} />);

        // Verify empty message
        // expect(screen.getByText(/no data/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should support pagination', async () => {
        const manyItems = Array.from({ length: 50 }, (_, i) => ({
          ...mockApiResponses.task.single,
          id: `test-id-${i}`,
        }));
        // render(<DynamicTable entityType="bus_task" data={manyItems} pageSize={10} />);

        // Verify pagination controls
        // expect(screen.getByRole('button', { name: /next/i })).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });
    });
  });

  describe('EmailMessage Components', () => {
    describe('EmailMessage Form', () => {
      it('should render all form fields', async () => {
        // const { container } = render(<DynamicForm entityType="bus_email_message" />);

        // Example assertions - uncomment when components are available
        // expect(screen.getByLabelText(/Contact Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Deal Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Thread Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Subject/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Body Text/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Body Html/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Direction/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Sent At/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Received At/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Opened At/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Open Count/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should validate required fields', async () => {
        // const { getByRole } = render(<DynamicForm entityType="bus_email_message" />);

        // Submit empty form
        // fireEvent.click(getByRole('button', { name: /submit/i }));

        // Check for validation errors
        // await waitFor(() => {
        //   expect(screen.getByText(/subject is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/direction is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/open_count is required/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form submission', async () => {
        const onSubmit = vi.fn();
        // render(<DynamicForm entityType="bus_email_message" onSubmit={onSubmit} />);

        // Fill form fields
        // fireEvent.change(screen.getByLabelText(/Contact Id/i), {
        //   target: { value: 'Test Contact Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/Deal Id/i), {
        //   target: { value: 'Test Deal Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/Thread Id/i), {
        //   target: { value: 'Test Thread Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/Subject/i), {
        //   target: { value: 'Test Subject' },
        // });
        // fireEvent.change(screen.getByLabelText(/Direction/i), {
        //   target: { value: 'Test Direction' },
        // });

        // Submit form
        // fireEvent.click(screen.getByRole('button', { name: /submit/i }));

        // Verify submission
        // await waitFor(() => {
        //   expect(onSubmit).toHaveBeenCalled();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form errors', async () => {
        // const onSubmit = vi.fn().mockRejectedValue(new Error('Submit failed'));
        // render(<DynamicForm entityType="bus_email_message" onSubmit={onSubmit} />);

        // Submit form with valid data
        // await waitFor(() => {
        //   expect(screen.getByText(/error/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should support edit mode', async () => {
        const existingData = mockApiResponses.emailMessage.single;
        // render(<DynamicForm entityType="bus_email_message" initialData={existingData} mode="edit" />);

        // Verify fields are pre-populated
        // expect(screen.getByLabelText(/Contact Id/i)).toHaveValue(existingData.contact_id);
        // expect(screen.getByLabelText(/Deal Id/i)).toHaveValue(existingData.deal_id);
        // expect(screen.getByLabelText(/Thread Id/i)).toHaveValue(existingData.thread_id);
        // expect(screen.getByLabelText(/Subject/i)).toHaveValue(existingData.subject);
        // expect(screen.getByLabelText(/Direction/i)).toHaveValue(existingData.direction);
        expect(true).toBe(true); // Placeholder
      });
    });

    describe('EmailMessage Table', () => {
      it('should render table with data', async () => {
        const data = mockApiResponses.emailMessage.list;
        // render(<DynamicTable entityType="bus_email_message" data={data} />);

        // Verify table headers
        // expect(screen.getByText(/Contact Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Deal Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Thread Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Subject/i)).toBeInTheDocument();
        // expect(screen.getByText(/Body Text/i)).toBeInTheDocument();
        // expect(screen.getByText(/Body Html/i)).toBeInTheDocument();
        // expect(screen.getByText(/Direction/i)).toBeInTheDocument();
        // expect(screen.getByText(/Sent At/i)).toBeInTheDocument();
        // expect(screen.getByText(/Received At/i)).toBeInTheDocument();
        // expect(screen.getByText(/Opened At/i)).toBeInTheDocument();
        // expect(screen.getByText(/Open Count/i)).toBeInTheDocument();

        // Verify data rows
        // expect(screen.getAllByRole('row')).toHaveLength(data.length + 1); // +1 for header
        expect(true).toBe(true); // Placeholder
      });

      it('should support sorting', async () => {
        const data = mockApiResponses.emailMessage.list;
        // render(<DynamicTable entityType="bus_email_message" data={data} />);

        // Click column header to sort
        // fireEvent.click(screen.getByText(/[object Object]\.displayName/i));

        // Verify sort order
        expect(true).toBe(true); // Placeholder
      });

      it('should support row selection', async () => {
        const data = mockApiResponses.emailMessage.list;
        const onSelect = vi.fn();
        // render(<DynamicTable entityType="bus_email_message" data={data} onSelect={onSelect} />);

        // Click row
        // fireEvent.click(screen.getAllByRole('row')[1]);

        // Verify selection callback
        // expect(onSelect).toHaveBeenCalledWith(data[0]);
        expect(true).toBe(true); // Placeholder
      });

      it('should render empty state', async () => {
        // render(<DynamicTable entityType="bus_email_message" data={[]} />);

        // Verify empty message
        // expect(screen.getByText(/no data/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should support pagination', async () => {
        const manyItems = Array.from({ length: 50 }, (_, i) => ({
          ...mockApiResponses.emailMessage.single,
          id: `test-id-${i}`,
        }));
        // render(<DynamicTable entityType="bus_email_message" data={manyItems} pageSize={10} />);

        // Verify pagination controls
        // expect(screen.getByRole('button', { name: /next/i })).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });
    });
  });

  describe('EmailTemplate Components', () => {
    describe('EmailTemplate Form', () => {
      it('should render all form fields', async () => {
        // const { container } = render(<DynamicForm entityType="bus_email_template" />);

        // Example assertions - uncomment when components are available
        // expect(screen.getByLabelText(/Name/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Subject/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Body Html/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Body Text/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Category/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Is Active/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should validate required fields', async () => {
        // const { getByRole } = render(<DynamicForm entityType="bus_email_template" />);

        // Submit empty form
        // fireEvent.click(getByRole('button', { name: /submit/i }));

        // Check for validation errors
        // await waitFor(() => {
        //   expect(screen.getByText(/name is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/subject is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/body_html is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/is_active is required/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form submission', async () => {
        const onSubmit = vi.fn();
        // render(<DynamicForm entityType="bus_email_template" onSubmit={onSubmit} />);

        // Fill form fields
        // fireEvent.change(screen.getByLabelText(/Name/i), {
        //   target: { value: 'Test Name' },
        // });
        // fireEvent.change(screen.getByLabelText(/Subject/i), {
        //   target: { value: 'Test Subject' },
        // });
        // fireEvent.change(screen.getByLabelText(/Category/i), {
        //   target: { value: 'Test Category' },
        // });

        // Submit form
        // fireEvent.click(screen.getByRole('button', { name: /submit/i }));

        // Verify submission
        // await waitFor(() => {
        //   expect(onSubmit).toHaveBeenCalled();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form errors', async () => {
        // const onSubmit = vi.fn().mockRejectedValue(new Error('Submit failed'));
        // render(<DynamicForm entityType="bus_email_template" onSubmit={onSubmit} />);

        // Submit form with valid data
        // await waitFor(() => {
        //   expect(screen.getByText(/error/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should support edit mode', async () => {
        const existingData = mockApiResponses.emailTemplate.single;
        // render(<DynamicForm entityType="bus_email_template" initialData={existingData} mode="edit" />);

        // Verify fields are pre-populated
        // expect(screen.getByLabelText(/Name/i)).toHaveValue(existingData.name);
        // expect(screen.getByLabelText(/Subject/i)).toHaveValue(existingData.subject);
        // expect(screen.getByLabelText(/Category/i)).toHaveValue(existingData.category);
        expect(true).toBe(true); // Placeholder
      });
    });

    describe('EmailTemplate Table', () => {
      it('should render table with data', async () => {
        const data = mockApiResponses.emailTemplate.list;
        // render(<DynamicTable entityType="bus_email_template" data={data} />);

        // Verify table headers
        // expect(screen.getByText(/Name/i)).toBeInTheDocument();
        // expect(screen.getByText(/Subject/i)).toBeInTheDocument();
        // expect(screen.getByText(/Body Html/i)).toBeInTheDocument();
        // expect(screen.getByText(/Body Text/i)).toBeInTheDocument();
        // expect(screen.getByText(/Category/i)).toBeInTheDocument();
        // expect(screen.getByText(/Is Active/i)).toBeInTheDocument();

        // Verify data rows
        // expect(screen.getAllByRole('row')).toHaveLength(data.length + 1); // +1 for header
        expect(true).toBe(true); // Placeholder
      });

      it('should support sorting', async () => {
        const data = mockApiResponses.emailTemplate.list;
        // render(<DynamicTable entityType="bus_email_template" data={data} />);

        // Click column header to sort
        // fireEvent.click(screen.getByText(/[object Object]\.displayName/i));

        // Verify sort order
        expect(true).toBe(true); // Placeholder
      });

      it('should support row selection', async () => {
        const data = mockApiResponses.emailTemplate.list;
        const onSelect = vi.fn();
        // render(<DynamicTable entityType="bus_email_template" data={data} onSelect={onSelect} />);

        // Click row
        // fireEvent.click(screen.getAllByRole('row')[1]);

        // Verify selection callback
        // expect(onSelect).toHaveBeenCalledWith(data[0]);
        expect(true).toBe(true); // Placeholder
      });

      it('should render empty state', async () => {
        // render(<DynamicTable entityType="bus_email_template" data={[]} />);

        // Verify empty message
        // expect(screen.getByText(/no data/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should support pagination', async () => {
        const manyItems = Array.from({ length: 50 }, (_, i) => ({
          ...mockApiResponses.emailTemplate.single,
          id: `test-id-${i}`,
        }));
        // render(<DynamicTable entityType="bus_email_template" data={manyItems} pageSize={10} />);

        // Verify pagination controls
        // expect(screen.getByRole('button', { name: /next/i })).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });
    });
  });

  describe('Product Components', () => {
    describe('Product Form', () => {
      it('should render all form fields', async () => {
        // const { container } = render(<DynamicForm entityType="bus_product" />);

        // Example assertions - uncomment when components are available
        // expect(screen.getByLabelText(/Name/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Sku/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Description/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Unit Price/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Currency/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Is Active/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should validate required fields', async () => {
        // const { getByRole } = render(<DynamicForm entityType="bus_product" />);

        // Submit empty form
        // fireEvent.click(getByRole('button', { name: /submit/i }));

        // Check for validation errors
        // await waitFor(() => {
        //   expect(screen.getByText(/name is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/sku is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/unit_price is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/currency is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/is_active is required/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form submission', async () => {
        const onSubmit = vi.fn();
        // render(<DynamicForm entityType="bus_product" onSubmit={onSubmit} />);

        // Fill form fields
        // fireEvent.change(screen.getByLabelText(/Name/i), {
        //   target: { value: 'Test Name' },
        // });
        // fireEvent.change(screen.getByLabelText(/Sku/i), {
        //   target: { value: 'Test Sku' },
        // });
        // fireEvent.change(screen.getByLabelText(/Currency/i), {
        //   target: { value: 'Test Currency' },
        // });

        // Submit form
        // fireEvent.click(screen.getByRole('button', { name: /submit/i }));

        // Verify submission
        // await waitFor(() => {
        //   expect(onSubmit).toHaveBeenCalled();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form errors', async () => {
        // const onSubmit = vi.fn().mockRejectedValue(new Error('Submit failed'));
        // render(<DynamicForm entityType="bus_product" onSubmit={onSubmit} />);

        // Submit form with valid data
        // await waitFor(() => {
        //   expect(screen.getByText(/error/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should support edit mode', async () => {
        const existingData = mockApiResponses.product.single;
        // render(<DynamicForm entityType="bus_product" initialData={existingData} mode="edit" />);

        // Verify fields are pre-populated
        // expect(screen.getByLabelText(/Name/i)).toHaveValue(existingData.name);
        // expect(screen.getByLabelText(/Sku/i)).toHaveValue(existingData.sku);
        // expect(screen.getByLabelText(/Currency/i)).toHaveValue(existingData.currency);
        expect(true).toBe(true); // Placeholder
      });
    });

    describe('Product Table', () => {
      it('should render table with data', async () => {
        const data = mockApiResponses.product.list;
        // render(<DynamicTable entityType="bus_product" data={data} />);

        // Verify table headers
        // expect(screen.getByText(/Name/i)).toBeInTheDocument();
        // expect(screen.getByText(/Sku/i)).toBeInTheDocument();
        // expect(screen.getByText(/Description/i)).toBeInTheDocument();
        // expect(screen.getByText(/Unit Price/i)).toBeInTheDocument();
        // expect(screen.getByText(/Currency/i)).toBeInTheDocument();
        // expect(screen.getByText(/Is Active/i)).toBeInTheDocument();

        // Verify data rows
        // expect(screen.getAllByRole('row')).toHaveLength(data.length + 1); // +1 for header
        expect(true).toBe(true); // Placeholder
      });

      it('should support sorting', async () => {
        const data = mockApiResponses.product.list;
        // render(<DynamicTable entityType="bus_product" data={data} />);

        // Click column header to sort
        // fireEvent.click(screen.getByText(/[object Object]\.displayName/i));

        // Verify sort order
        expect(true).toBe(true); // Placeholder
      });

      it('should support row selection', async () => {
        const data = mockApiResponses.product.list;
        const onSelect = vi.fn();
        // render(<DynamicTable entityType="bus_product" data={data} onSelect={onSelect} />);

        // Click row
        // fireEvent.click(screen.getAllByRole('row')[1]);

        // Verify selection callback
        // expect(onSelect).toHaveBeenCalledWith(data[0]);
        expect(true).toBe(true); // Placeholder
      });

      it('should render empty state', async () => {
        // render(<DynamicTable entityType="bus_product" data={[]} />);

        // Verify empty message
        // expect(screen.getByText(/no data/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should support pagination', async () => {
        const manyItems = Array.from({ length: 50 }, (_, i) => ({
          ...mockApiResponses.product.single,
          id: `test-id-${i}`,
        }));
        // render(<DynamicTable entityType="bus_product" data={manyItems} pageSize={10} />);

        // Verify pagination controls
        // expect(screen.getByRole('button', { name: /next/i })).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });
    });
  });

  describe('Quote Components', () => {
    describe('Quote Form', () => {
      it('should render all form fields', async () => {
        // const { container } = render(<DynamicForm entityType="bus_quote" />);

        // Example assertions - uncomment when components are available
        // expect(screen.getByLabelText(/Deal Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Quote Number/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Status/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Valid Until/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Subtotal/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Discount Amount/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Tax Amount/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Total Amount/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Terms/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Notes/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should validate required fields', async () => {
        // const { getByRole } = render(<DynamicForm entityType="bus_quote" />);

        // Submit empty form
        // fireEvent.click(getByRole('button', { name: /submit/i }));

        // Check for validation errors
        // await waitFor(() => {
        //   expect(screen.getByText(/deal_id is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/quote_number is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/status is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/subtotal is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/discount_amount is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/tax_amount is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/total_amount is required/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form submission', async () => {
        const onSubmit = vi.fn();
        // render(<DynamicForm entityType="bus_quote" onSubmit={onSubmit} />);

        // Fill form fields
        // fireEvent.change(screen.getByLabelText(/Deal Id/i), {
        //   target: { value: 'Test Deal Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/Quote Number/i), {
        //   target: { value: 'Test Quote Number' },
        // });
        // fireEvent.change(screen.getByLabelText(/Status/i), {
        //   target: { value: 'Test Status' },
        // });

        // Submit form
        // fireEvent.click(screen.getByRole('button', { name: /submit/i }));

        // Verify submission
        // await waitFor(() => {
        //   expect(onSubmit).toHaveBeenCalled();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form errors', async () => {
        // const onSubmit = vi.fn().mockRejectedValue(new Error('Submit failed'));
        // render(<DynamicForm entityType="bus_quote" onSubmit={onSubmit} />);

        // Submit form with valid data
        // await waitFor(() => {
        //   expect(screen.getByText(/error/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should support edit mode', async () => {
        const existingData = mockApiResponses.quote.single;
        // render(<DynamicForm entityType="bus_quote" initialData={existingData} mode="edit" />);

        // Verify fields are pre-populated
        // expect(screen.getByLabelText(/Deal Id/i)).toHaveValue(existingData.deal_id);
        // expect(screen.getByLabelText(/Quote Number/i)).toHaveValue(existingData.quote_number);
        // expect(screen.getByLabelText(/Status/i)).toHaveValue(existingData.status);
        expect(true).toBe(true); // Placeholder
      });
    });

    describe('Quote Table', () => {
      it('should render table with data', async () => {
        const data = mockApiResponses.quote.list;
        // render(<DynamicTable entityType="bus_quote" data={data} />);

        // Verify table headers
        // expect(screen.getByText(/Deal Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Quote Number/i)).toBeInTheDocument();
        // expect(screen.getByText(/Status/i)).toBeInTheDocument();
        // expect(screen.getByText(/Valid Until/i)).toBeInTheDocument();
        // expect(screen.getByText(/Subtotal/i)).toBeInTheDocument();
        // expect(screen.getByText(/Discount Amount/i)).toBeInTheDocument();
        // expect(screen.getByText(/Tax Amount/i)).toBeInTheDocument();
        // expect(screen.getByText(/Total Amount/i)).toBeInTheDocument();
        // expect(screen.getByText(/Terms/i)).toBeInTheDocument();
        // expect(screen.getByText(/Notes/i)).toBeInTheDocument();

        // Verify data rows
        // expect(screen.getAllByRole('row')).toHaveLength(data.length + 1); // +1 for header
        expect(true).toBe(true); // Placeholder
      });

      it('should support sorting', async () => {
        const data = mockApiResponses.quote.list;
        // render(<DynamicTable entityType="bus_quote" data={data} />);

        // Click column header to sort
        // fireEvent.click(screen.getByText(/[object Object]\.displayName/i));

        // Verify sort order
        expect(true).toBe(true); // Placeholder
      });

      it('should support row selection', async () => {
        const data = mockApiResponses.quote.list;
        const onSelect = vi.fn();
        // render(<DynamicTable entityType="bus_quote" data={data} onSelect={onSelect} />);

        // Click row
        // fireEvent.click(screen.getAllByRole('row')[1]);

        // Verify selection callback
        // expect(onSelect).toHaveBeenCalledWith(data[0]);
        expect(true).toBe(true); // Placeholder
      });

      it('should render empty state', async () => {
        // render(<DynamicTable entityType="bus_quote" data={[]} />);

        // Verify empty message
        // expect(screen.getByText(/no data/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should support pagination', async () => {
        const manyItems = Array.from({ length: 50 }, (_, i) => ({
          ...mockApiResponses.quote.single,
          id: `test-id-${i}`,
        }));
        // render(<DynamicTable entityType="bus_quote" data={manyItems} pageSize={10} />);

        // Verify pagination controls
        // expect(screen.getByRole('button', { name: /next/i })).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });
    });
  });

  describe('QuoteItem Components', () => {
    describe('QuoteItem Form', () => {
      it('should render all form fields', async () => {
        // const { container } = render(<DynamicForm entityType="bus_quote_item" />);

        // Example assertions - uncomment when components are available
        // expect(screen.getByLabelText(/Quote Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Product Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Description/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Quantity/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Unit Price/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Discount Percent/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Total Price/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should validate required fields', async () => {
        // const { getByRole } = render(<DynamicForm entityType="bus_quote_item" />);

        // Submit empty form
        // fireEvent.click(getByRole('button', { name: /submit/i }));

        // Check for validation errors
        // await waitFor(() => {
        //   expect(screen.getByText(/quote_id is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/product_id is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/quantity is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/unit_price is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/discount_percent is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/total_price is required/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form submission', async () => {
        const onSubmit = vi.fn();
        // render(<DynamicForm entityType="bus_quote_item" onSubmit={onSubmit} />);

        // Fill form fields
        // fireEvent.change(screen.getByLabelText(/Quote Id/i), {
        //   target: { value: 'Test Quote Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/Product Id/i), {
        //   target: { value: 'Test Product Id' },
        // });
        // fireEvent.change(screen.getByLabelText(/Description/i), {
        //   target: { value: 'Test Description' },
        // });

        // Submit form
        // fireEvent.click(screen.getByRole('button', { name: /submit/i }));

        // Verify submission
        // await waitFor(() => {
        //   expect(onSubmit).toHaveBeenCalled();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form errors', async () => {
        // const onSubmit = vi.fn().mockRejectedValue(new Error('Submit failed'));
        // render(<DynamicForm entityType="bus_quote_item" onSubmit={onSubmit} />);

        // Submit form with valid data
        // await waitFor(() => {
        //   expect(screen.getByText(/error/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should support edit mode', async () => {
        const existingData = mockApiResponses.quoteItem.single;
        // render(<DynamicForm entityType="bus_quote_item" initialData={existingData} mode="edit" />);

        // Verify fields are pre-populated
        // expect(screen.getByLabelText(/Quote Id/i)).toHaveValue(existingData.quote_id);
        // expect(screen.getByLabelText(/Product Id/i)).toHaveValue(existingData.product_id);
        // expect(screen.getByLabelText(/Description/i)).toHaveValue(existingData.description);
        expect(true).toBe(true); // Placeholder
      });
    });

    describe('QuoteItem Table', () => {
      it('should render table with data', async () => {
        const data = mockApiResponses.quoteItem.list;
        // render(<DynamicTable entityType="bus_quote_item" data={data} />);

        // Verify table headers
        // expect(screen.getByText(/Quote Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Product Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Description/i)).toBeInTheDocument();
        // expect(screen.getByText(/Quantity/i)).toBeInTheDocument();
        // expect(screen.getByText(/Unit Price/i)).toBeInTheDocument();
        // expect(screen.getByText(/Discount Percent/i)).toBeInTheDocument();
        // expect(screen.getByText(/Total Price/i)).toBeInTheDocument();

        // Verify data rows
        // expect(screen.getAllByRole('row')).toHaveLength(data.length + 1); // +1 for header
        expect(true).toBe(true); // Placeholder
      });

      it('should support sorting', async () => {
        const data = mockApiResponses.quoteItem.list;
        // render(<DynamicTable entityType="bus_quote_item" data={data} />);

        // Click column header to sort
        // fireEvent.click(screen.getByText(/[object Object]\.displayName/i));

        // Verify sort order
        expect(true).toBe(true); // Placeholder
      });

      it('should support row selection', async () => {
        const data = mockApiResponses.quoteItem.list;
        const onSelect = vi.fn();
        // render(<DynamicTable entityType="bus_quote_item" data={data} onSelect={onSelect} />);

        // Click row
        // fireEvent.click(screen.getAllByRole('row')[1]);

        // Verify selection callback
        // expect(onSelect).toHaveBeenCalledWith(data[0]);
        expect(true).toBe(true); // Placeholder
      });

      it('should render empty state', async () => {
        // render(<DynamicTable entityType="bus_quote_item" data={[]} />);

        // Verify empty message
        // expect(screen.getByText(/no data/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should support pagination', async () => {
        const manyItems = Array.from({ length: 50 }, (_, i) => ({
          ...mockApiResponses.quoteItem.single,
          id: `test-id-${i}`,
        }));
        // render(<DynamicTable entityType="bus_quote_item" data={manyItems} pageSize={10} />);

        // Verify pagination controls
        // expect(screen.getByRole('button', { name: /next/i })).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });
    });
  });

  describe('User Components', () => {
    describe('User Form', () => {
      it('should render all form fields', async () => {
        // const { container } = render(<DynamicForm entityType="bus_user" />);

        // Example assertions - uncomment when components are available
        // expect(screen.getByLabelText(/Email/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/First Name/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Last Name/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Role/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Team Id/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Is Active/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Last Login/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should validate required fields', async () => {
        // const { getByRole } = render(<DynamicForm entityType="bus_user" />);

        // Submit empty form
        // fireEvent.click(getByRole('button', { name: /submit/i }));

        // Check for validation errors
        // await waitFor(() => {
        //   expect(screen.getByText(/email is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/first_name is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/last_name is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/role is required/i)).toBeInTheDocument();
        // });
        // await waitFor(() => {
        //   expect(screen.getByText(/is_active is required/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form submission', async () => {
        const onSubmit = vi.fn();
        // render(<DynamicForm entityType="bus_user" onSubmit={onSubmit} />);

        // Fill form fields
        // fireEvent.change(screen.getByLabelText(/Email/i), {
        //   target: { value: 'Test Email' },
        // });
        // fireEvent.change(screen.getByLabelText(/First Name/i), {
        //   target: { value: 'Test First Name' },
        // });
        // fireEvent.change(screen.getByLabelText(/Last Name/i), {
        //   target: { value: 'Test Last Name' },
        // });
        // fireEvent.change(screen.getByLabelText(/Role/i), {
        //   target: { value: 'Test Role' },
        // });
        // fireEvent.change(screen.getByLabelText(/Team Id/i), {
        //   target: { value: 'Test Team Id' },
        // });

        // Submit form
        // fireEvent.click(screen.getByRole('button', { name: /submit/i }));

        // Verify submission
        // await waitFor(() => {
        //   expect(onSubmit).toHaveBeenCalled();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form errors', async () => {
        // const onSubmit = vi.fn().mockRejectedValue(new Error('Submit failed'));
        // render(<DynamicForm entityType="bus_user" onSubmit={onSubmit} />);

        // Submit form with valid data
        // await waitFor(() => {
        //   expect(screen.getByText(/error/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should support edit mode', async () => {
        const existingData = mockApiResponses.user.single;
        // render(<DynamicForm entityType="bus_user" initialData={existingData} mode="edit" />);

        // Verify fields are pre-populated
        // expect(screen.getByLabelText(/Email/i)).toHaveValue(existingData.email);
        // expect(screen.getByLabelText(/First Name/i)).toHaveValue(existingData.first_name);
        // expect(screen.getByLabelText(/Last Name/i)).toHaveValue(existingData.last_name);
        // expect(screen.getByLabelText(/Role/i)).toHaveValue(existingData.role);
        // expect(screen.getByLabelText(/Team Id/i)).toHaveValue(existingData.team_id);
        expect(true).toBe(true); // Placeholder
      });
    });

    describe('User Table', () => {
      it('should render table with data', async () => {
        const data = mockApiResponses.user.list;
        // render(<DynamicTable entityType="bus_user" data={data} />);

        // Verify table headers
        // expect(screen.getByText(/Email/i)).toBeInTheDocument();
        // expect(screen.getByText(/First Name/i)).toBeInTheDocument();
        // expect(screen.getByText(/Last Name/i)).toBeInTheDocument();
        // expect(screen.getByText(/Role/i)).toBeInTheDocument();
        // expect(screen.getByText(/Team Id/i)).toBeInTheDocument();
        // expect(screen.getByText(/Is Active/i)).toBeInTheDocument();
        // expect(screen.getByText(/Last Login/i)).toBeInTheDocument();

        // Verify data rows
        // expect(screen.getAllByRole('row')).toHaveLength(data.length + 1); // +1 for header
        expect(true).toBe(true); // Placeholder
      });

      it('should support sorting', async () => {
        const data = mockApiResponses.user.list;
        // render(<DynamicTable entityType="bus_user" data={data} />);

        // Click column header to sort
        // fireEvent.click(screen.getByText(/[object Object]\.displayName/i));

        // Verify sort order
        expect(true).toBe(true); // Placeholder
      });

      it('should support row selection', async () => {
        const data = mockApiResponses.user.list;
        const onSelect = vi.fn();
        // render(<DynamicTable entityType="bus_user" data={data} onSelect={onSelect} />);

        // Click row
        // fireEvent.click(screen.getAllByRole('row')[1]);

        // Verify selection callback
        // expect(onSelect).toHaveBeenCalledWith(data[0]);
        expect(true).toBe(true); // Placeholder
      });

      it('should render empty state', async () => {
        // render(<DynamicTable entityType="bus_user" data={[]} />);

        // Verify empty message
        // expect(screen.getByText(/no data/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should support pagination', async () => {
        const manyItems = Array.from({ length: 50 }, (_, i) => ({
          ...mockApiResponses.user.single,
          id: `test-id-${i}`,
        }));
        // render(<DynamicTable entityType="bus_user" data={manyItems} pageSize={10} />);

        // Verify pagination controls
        // expect(screen.getByRole('button', { name: /next/i })).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });
    });
  });

  describe('Team Components', () => {
    describe('Team Form', () => {
      it('should render all form fields', async () => {
        // const { container } = render(<DynamicForm entityType="bus_team" />);

        // Example assertions - uncomment when components are available
        // expect(screen.getByLabelText(/Name/i)).toBeInTheDocument();
        // expect(screen.getByLabelText(/Manager Id/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should validate required fields', async () => {
        // const { getByRole } = render(<DynamicForm entityType="bus_team" />);

        // Submit empty form
        // fireEvent.click(getByRole('button', { name: /submit/i }));

        // Check for validation errors
        // await waitFor(() => {
        //   expect(screen.getByText(/name is required/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form submission', async () => {
        const onSubmit = vi.fn();
        // render(<DynamicForm entityType="bus_team" onSubmit={onSubmit} />);

        // Fill form fields
        // fireEvent.change(screen.getByLabelText(/Name/i), {
        //   target: { value: 'Test Name' },
        // });
        // fireEvent.change(screen.getByLabelText(/Manager Id/i), {
        //   target: { value: 'Test Manager Id' },
        // });

        // Submit form
        // fireEvent.click(screen.getByRole('button', { name: /submit/i }));

        // Verify submission
        // await waitFor(() => {
        //   expect(onSubmit).toHaveBeenCalled();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should handle form errors', async () => {
        // const onSubmit = vi.fn().mockRejectedValue(new Error('Submit failed'));
        // render(<DynamicForm entityType="bus_team" onSubmit={onSubmit} />);

        // Submit form with valid data
        // await waitFor(() => {
        //   expect(screen.getByText(/error/i)).toBeInTheDocument();
        // });
        expect(true).toBe(true); // Placeholder
      });

      it('should support edit mode', async () => {
        const existingData = mockApiResponses.team.single;
        // render(<DynamicForm entityType="bus_team" initialData={existingData} mode="edit" />);

        // Verify fields are pre-populated
        // expect(screen.getByLabelText(/Name/i)).toHaveValue(existingData.name);
        // expect(screen.getByLabelText(/Manager Id/i)).toHaveValue(existingData.manager_id);
        expect(true).toBe(true); // Placeholder
      });
    });

    describe('Team Table', () => {
      it('should render table with data', async () => {
        const data = mockApiResponses.team.list;
        // render(<DynamicTable entityType="bus_team" data={data} />);

        // Verify table headers
        // expect(screen.getByText(/Name/i)).toBeInTheDocument();
        // expect(screen.getByText(/Manager Id/i)).toBeInTheDocument();

        // Verify data rows
        // expect(screen.getAllByRole('row')).toHaveLength(data.length + 1); // +1 for header
        expect(true).toBe(true); // Placeholder
      });

      it('should support sorting', async () => {
        const data = mockApiResponses.team.list;
        // render(<DynamicTable entityType="bus_team" data={data} />);

        // Click column header to sort
        // fireEvent.click(screen.getByText(/[object Object]\.displayName/i));

        // Verify sort order
        expect(true).toBe(true); // Placeholder
      });

      it('should support row selection', async () => {
        const data = mockApiResponses.team.list;
        const onSelect = vi.fn();
        // render(<DynamicTable entityType="bus_team" data={data} onSelect={onSelect} />);

        // Click row
        // fireEvent.click(screen.getAllByRole('row')[1]);

        // Verify selection callback
        // expect(onSelect).toHaveBeenCalledWith(data[0]);
        expect(true).toBe(true); // Placeholder
      });

      it('should render empty state', async () => {
        // render(<DynamicTable entityType="bus_team" data={[]} />);

        // Verify empty message
        // expect(screen.getByText(/no data/i)).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });

      it('should support pagination', async () => {
        const manyItems = Array.from({ length: 50 }, (_, i) => ({
          ...mockApiResponses.team.single,
          id: `test-id-${i}`,
        }));
        // render(<DynamicTable entityType="bus_team" data={manyItems} pageSize={10} />);

        // Verify pagination controls
        // expect(screen.getByRole('button', { name: /next/i })).toBeInTheDocument();
        expect(true).toBe(true); // Placeholder
      });
    });
  });

});

describe('CRUD Operations Integration', () => {
  describe('Company CRUD', () => {
    it('should create Company', async () => {
      global.fetch = createMockFetch({
        '/api/companies': mockApiResponses.company.single,
      });

      // Simulate create operation
      const response = await fetch('/api/companies', {
        method: 'POST',
        body: JSON.stringify(mockApiResponses.company.single),
      });

      expect(response.ok).toBe(true);
      const data = await response.json();
      expect(data.id).toBeDefined();
    });

    it('should read Companies', async () => {
      global.fetch = createMockFetch({
        '/api/companies': mockApiResponses.company.list,
      });

      const response = await fetch('/api/companies');
      expect(response.ok).toBe(true);

      const data = await response.json();
      expect(Array.isArray(data)).toBe(true);
    });

    it('should update Company', async () => {
      global.fetch = createMockFetch({
        '/api/companies/test-id-1': mockApiResponses.company.single,
      });

      const response = await fetch('/api/companies/test-id-1', {
        method: 'PATCH',
        body: JSON.stringify({ /* update data */ }),
      });

      expect(response.ok).toBe(true);
    });

    it('should delete Company', async () => {
      global.fetch = createMockFetch({
        '/api/companies/test-id-1': { success: true },
      });

      const response = await fetch('/api/companies/test-id-1', {
        method: 'DELETE',
      });

      expect(response.ok).toBe(true);
    });
  });

  describe('Contact CRUD', () => {
    it('should create Contact', async () => {
      global.fetch = createMockFetch({
        '/api/contacts': mockApiResponses.contact.single,
      });

      // Simulate create operation
      const response = await fetch('/api/contacts', {
        method: 'POST',
        body: JSON.stringify(mockApiResponses.contact.single),
      });

      expect(response.ok).toBe(true);
      const data = await response.json();
      expect(data.id).toBeDefined();
    });

    it('should read Contacts', async () => {
      global.fetch = createMockFetch({
        '/api/contacts': mockApiResponses.contact.list,
      });

      const response = await fetch('/api/contacts');
      expect(response.ok).toBe(true);

      const data = await response.json();
      expect(Array.isArray(data)).toBe(true);
    });

    it('should update Contact', async () => {
      global.fetch = createMockFetch({
        '/api/contacts/test-id-1': mockApiResponses.contact.single,
      });

      const response = await fetch('/api/contacts/test-id-1', {
        method: 'PATCH',
        body: JSON.stringify({ /* update data */ }),
      });

      expect(response.ok).toBe(true);
    });

    it('should delete Contact', async () => {
      global.fetch = createMockFetch({
        '/api/contacts/test-id-1': { success: true },
      });

      const response = await fetch('/api/contacts/test-id-1', {
        method: 'DELETE',
      });

      expect(response.ok).toBe(true);
    });
  });

  describe('Deal CRUD', () => {
    it('should create Deal', async () => {
      global.fetch = createMockFetch({
        '/api/deals': mockApiResponses.deal.single,
      });

      // Simulate create operation
      const response = await fetch('/api/deals', {
        method: 'POST',
        body: JSON.stringify(mockApiResponses.deal.single),
      });

      expect(response.ok).toBe(true);
      const data = await response.json();
      expect(data.id).toBeDefined();
    });

    it('should read Deals', async () => {
      global.fetch = createMockFetch({
        '/api/deals': mockApiResponses.deal.list,
      });

      const response = await fetch('/api/deals');
      expect(response.ok).toBe(true);

      const data = await response.json();
      expect(Array.isArray(data)).toBe(true);
    });

    it('should update Deal', async () => {
      global.fetch = createMockFetch({
        '/api/deals/test-id-1': mockApiResponses.deal.single,
      });

      const response = await fetch('/api/deals/test-id-1', {
        method: 'PATCH',
        body: JSON.stringify({ /* update data */ }),
      });

      expect(response.ok).toBe(true);
    });

    it('should delete Deal', async () => {
      global.fetch = createMockFetch({
        '/api/deals/test-id-1': { success: true },
      });

      const response = await fetch('/api/deals/test-id-1', {
        method: 'DELETE',
      });

      expect(response.ok).toBe(true);
    });
  });

  describe('DealStage CRUD', () => {
    it('should create DealStage', async () => {
      global.fetch = createMockFetch({
        '/api/deal-stages': mockApiResponses.dealStage.single,
      });

      // Simulate create operation
      const response = await fetch('/api/deal-stages', {
        method: 'POST',
        body: JSON.stringify(mockApiResponses.dealStage.single),
      });

      expect(response.ok).toBe(true);
      const data = await response.json();
      expect(data.id).toBeDefined();
    });

    it('should read DealStages', async () => {
      global.fetch = createMockFetch({
        '/api/deal-stages': mockApiResponses.dealStage.list,
      });

      const response = await fetch('/api/deal-stages');
      expect(response.ok).toBe(true);

      const data = await response.json();
      expect(Array.isArray(data)).toBe(true);
    });

    it('should update DealStage', async () => {
      global.fetch = createMockFetch({
        '/api/deal-stages/test-id-1': mockApiResponses.dealStage.single,
      });

      const response = await fetch('/api/deal-stages/test-id-1', {
        method: 'PATCH',
        body: JSON.stringify({ /* update data */ }),
      });

      expect(response.ok).toBe(true);
    });

    it('should delete DealStage', async () => {
      global.fetch = createMockFetch({
        '/api/deal-stages/test-id-1': { success: true },
      });

      const response = await fetch('/api/deal-stages/test-id-1', {
        method: 'DELETE',
      });

      expect(response.ok).toBe(true);
    });
  });

  describe('Pipeline CRUD', () => {
    it('should create Pipeline', async () => {
      global.fetch = createMockFetch({
        '/api/pipelines': mockApiResponses.pipeline.single,
      });

      // Simulate create operation
      const response = await fetch('/api/pipelines', {
        method: 'POST',
        body: JSON.stringify(mockApiResponses.pipeline.single),
      });

      expect(response.ok).toBe(true);
      const data = await response.json();
      expect(data.id).toBeDefined();
    });

    it('should read Pipelines', async () => {
      global.fetch = createMockFetch({
        '/api/pipelines': mockApiResponses.pipeline.list,
      });

      const response = await fetch('/api/pipelines');
      expect(response.ok).toBe(true);

      const data = await response.json();
      expect(Array.isArray(data)).toBe(true);
    });

    it('should update Pipeline', async () => {
      global.fetch = createMockFetch({
        '/api/pipelines/test-id-1': mockApiResponses.pipeline.single,
      });

      const response = await fetch('/api/pipelines/test-id-1', {
        method: 'PATCH',
        body: JSON.stringify({ /* update data */ }),
      });

      expect(response.ok).toBe(true);
    });

    it('should delete Pipeline', async () => {
      global.fetch = createMockFetch({
        '/api/pipelines/test-id-1': { success: true },
      });

      const response = await fetch('/api/pipelines/test-id-1', {
        method: 'DELETE',
      });

      expect(response.ok).toBe(true);
    });
  });

  describe('Activity CRUD', () => {
    it('should create Activity', async () => {
      global.fetch = createMockFetch({
        '/api/activities': mockApiResponses.activity.single,
      });

      // Simulate create operation
      const response = await fetch('/api/activities', {
        method: 'POST',
        body: JSON.stringify(mockApiResponses.activity.single),
      });

      expect(response.ok).toBe(true);
      const data = await response.json();
      expect(data.id).toBeDefined();
    });

    it('should read Activities', async () => {
      global.fetch = createMockFetch({
        '/api/activities': mockApiResponses.activity.list,
      });

      const response = await fetch('/api/activities');
      expect(response.ok).toBe(true);

      const data = await response.json();
      expect(Array.isArray(data)).toBe(true);
    });

    it('should update Activity', async () => {
      global.fetch = createMockFetch({
        '/api/activities/test-id-1': mockApiResponses.activity.single,
      });

      const response = await fetch('/api/activities/test-id-1', {
        method: 'PATCH',
        body: JSON.stringify({ /* update data */ }),
      });

      expect(response.ok).toBe(true);
    });

    it('should delete Activity', async () => {
      global.fetch = createMockFetch({
        '/api/activities/test-id-1': { success: true },
      });

      const response = await fetch('/api/activities/test-id-1', {
        method: 'DELETE',
      });

      expect(response.ok).toBe(true);
    });
  });

  describe('Note CRUD', () => {
    it('should create Note', async () => {
      global.fetch = createMockFetch({
        '/api/notes': mockApiResponses.note.single,
      });

      // Simulate create operation
      const response = await fetch('/api/notes', {
        method: 'POST',
        body: JSON.stringify(mockApiResponses.note.single),
      });

      expect(response.ok).toBe(true);
      const data = await response.json();
      expect(data.id).toBeDefined();
    });

    it('should read Notes', async () => {
      global.fetch = createMockFetch({
        '/api/notes': mockApiResponses.note.list,
      });

      const response = await fetch('/api/notes');
      expect(response.ok).toBe(true);

      const data = await response.json();
      expect(Array.isArray(data)).toBe(true);
    });

    it('should update Note', async () => {
      global.fetch = createMockFetch({
        '/api/notes/test-id-1': mockApiResponses.note.single,
      });

      const response = await fetch('/api/notes/test-id-1', {
        method: 'PATCH',
        body: JSON.stringify({ /* update data */ }),
      });

      expect(response.ok).toBe(true);
    });

    it('should delete Note', async () => {
      global.fetch = createMockFetch({
        '/api/notes/test-id-1': { success: true },
      });

      const response = await fetch('/api/notes/test-id-1', {
        method: 'DELETE',
      });

      expect(response.ok).toBe(true);
    });
  });

  describe('Task CRUD', () => {
    it('should create Task', async () => {
      global.fetch = createMockFetch({
        '/api/tasks': mockApiResponses.task.single,
      });

      // Simulate create operation
      const response = await fetch('/api/tasks', {
        method: 'POST',
        body: JSON.stringify(mockApiResponses.task.single),
      });

      expect(response.ok).toBe(true);
      const data = await response.json();
      expect(data.id).toBeDefined();
    });

    it('should read Tasks', async () => {
      global.fetch = createMockFetch({
        '/api/tasks': mockApiResponses.task.list,
      });

      const response = await fetch('/api/tasks');
      expect(response.ok).toBe(true);

      const data = await response.json();
      expect(Array.isArray(data)).toBe(true);
    });

    it('should update Task', async () => {
      global.fetch = createMockFetch({
        '/api/tasks/test-id-1': mockApiResponses.task.single,
      });

      const response = await fetch('/api/tasks/test-id-1', {
        method: 'PATCH',
        body: JSON.stringify({ /* update data */ }),
      });

      expect(response.ok).toBe(true);
    });

    it('should delete Task', async () => {
      global.fetch = createMockFetch({
        '/api/tasks/test-id-1': { success: true },
      });

      const response = await fetch('/api/tasks/test-id-1', {
        method: 'DELETE',
      });

      expect(response.ok).toBe(true);
    });
  });

  describe('EmailMessage CRUD', () => {
    it('should create EmailMessage', async () => {
      global.fetch = createMockFetch({
        '/api/email-messages': mockApiResponses.emailMessage.single,
      });

      // Simulate create operation
      const response = await fetch('/api/email-messages', {
        method: 'POST',
        body: JSON.stringify(mockApiResponses.emailMessage.single),
      });

      expect(response.ok).toBe(true);
      const data = await response.json();
      expect(data.id).toBeDefined();
    });

    it('should read EmailMessages', async () => {
      global.fetch = createMockFetch({
        '/api/email-messages': mockApiResponses.emailMessage.list,
      });

      const response = await fetch('/api/email-messages');
      expect(response.ok).toBe(true);

      const data = await response.json();
      expect(Array.isArray(data)).toBe(true);
    });

    it('should update EmailMessage', async () => {
      global.fetch = createMockFetch({
        '/api/email-messages/test-id-1': mockApiResponses.emailMessage.single,
      });

      const response = await fetch('/api/email-messages/test-id-1', {
        method: 'PATCH',
        body: JSON.stringify({ /* update data */ }),
      });

      expect(response.ok).toBe(true);
    });

    it('should delete EmailMessage', async () => {
      global.fetch = createMockFetch({
        '/api/email-messages/test-id-1': { success: true },
      });

      const response = await fetch('/api/email-messages/test-id-1', {
        method: 'DELETE',
      });

      expect(response.ok).toBe(true);
    });
  });

  describe('EmailTemplate CRUD', () => {
    it('should create EmailTemplate', async () => {
      global.fetch = createMockFetch({
        '/api/email-templates': mockApiResponses.emailTemplate.single,
      });

      // Simulate create operation
      const response = await fetch('/api/email-templates', {
        method: 'POST',
        body: JSON.stringify(mockApiResponses.emailTemplate.single),
      });

      expect(response.ok).toBe(true);
      const data = await response.json();
      expect(data.id).toBeDefined();
    });

    it('should read EmailTemplates', async () => {
      global.fetch = createMockFetch({
        '/api/email-templates': mockApiResponses.emailTemplate.list,
      });

      const response = await fetch('/api/email-templates');
      expect(response.ok).toBe(true);

      const data = await response.json();
      expect(Array.isArray(data)).toBe(true);
    });

    it('should update EmailTemplate', async () => {
      global.fetch = createMockFetch({
        '/api/email-templates/test-id-1': mockApiResponses.emailTemplate.single,
      });

      const response = await fetch('/api/email-templates/test-id-1', {
        method: 'PATCH',
        body: JSON.stringify({ /* update data */ }),
      });

      expect(response.ok).toBe(true);
    });

    it('should delete EmailTemplate', async () => {
      global.fetch = createMockFetch({
        '/api/email-templates/test-id-1': { success: true },
      });

      const response = await fetch('/api/email-templates/test-id-1', {
        method: 'DELETE',
      });

      expect(response.ok).toBe(true);
    });
  });

  describe('Product CRUD', () => {
    it('should create Product', async () => {
      global.fetch = createMockFetch({
        '/api/products': mockApiResponses.product.single,
      });

      // Simulate create operation
      const response = await fetch('/api/products', {
        method: 'POST',
        body: JSON.stringify(mockApiResponses.product.single),
      });

      expect(response.ok).toBe(true);
      const data = await response.json();
      expect(data.id).toBeDefined();
    });

    it('should read Products', async () => {
      global.fetch = createMockFetch({
        '/api/products': mockApiResponses.product.list,
      });

      const response = await fetch('/api/products');
      expect(response.ok).toBe(true);

      const data = await response.json();
      expect(Array.isArray(data)).toBe(true);
    });

    it('should update Product', async () => {
      global.fetch = createMockFetch({
        '/api/products/test-id-1': mockApiResponses.product.single,
      });

      const response = await fetch('/api/products/test-id-1', {
        method: 'PATCH',
        body: JSON.stringify({ /* update data */ }),
      });

      expect(response.ok).toBe(true);
    });

    it('should delete Product', async () => {
      global.fetch = createMockFetch({
        '/api/products/test-id-1': { success: true },
      });

      const response = await fetch('/api/products/test-id-1', {
        method: 'DELETE',
      });

      expect(response.ok).toBe(true);
    });
  });

  describe('Quote CRUD', () => {
    it('should create Quote', async () => {
      global.fetch = createMockFetch({
        '/api/quotes': mockApiResponses.quote.single,
      });

      // Simulate create operation
      const response = await fetch('/api/quotes', {
        method: 'POST',
        body: JSON.stringify(mockApiResponses.quote.single),
      });

      expect(response.ok).toBe(true);
      const data = await response.json();
      expect(data.id).toBeDefined();
    });

    it('should read Quotes', async () => {
      global.fetch = createMockFetch({
        '/api/quotes': mockApiResponses.quote.list,
      });

      const response = await fetch('/api/quotes');
      expect(response.ok).toBe(true);

      const data = await response.json();
      expect(Array.isArray(data)).toBe(true);
    });

    it('should update Quote', async () => {
      global.fetch = createMockFetch({
        '/api/quotes/test-id-1': mockApiResponses.quote.single,
      });

      const response = await fetch('/api/quotes/test-id-1', {
        method: 'PATCH',
        body: JSON.stringify({ /* update data */ }),
      });

      expect(response.ok).toBe(true);
    });

    it('should delete Quote', async () => {
      global.fetch = createMockFetch({
        '/api/quotes/test-id-1': { success: true },
      });

      const response = await fetch('/api/quotes/test-id-1', {
        method: 'DELETE',
      });

      expect(response.ok).toBe(true);
    });
  });

  describe('QuoteItem CRUD', () => {
    it('should create QuoteItem', async () => {
      global.fetch = createMockFetch({
        '/api/quote-items': mockApiResponses.quoteItem.single,
      });

      // Simulate create operation
      const response = await fetch('/api/quote-items', {
        method: 'POST',
        body: JSON.stringify(mockApiResponses.quoteItem.single),
      });

      expect(response.ok).toBe(true);
      const data = await response.json();
      expect(data.id).toBeDefined();
    });

    it('should read QuoteItems', async () => {
      global.fetch = createMockFetch({
        '/api/quote-items': mockApiResponses.quoteItem.list,
      });

      const response = await fetch('/api/quote-items');
      expect(response.ok).toBe(true);

      const data = await response.json();
      expect(Array.isArray(data)).toBe(true);
    });

    it('should update QuoteItem', async () => {
      global.fetch = createMockFetch({
        '/api/quote-items/test-id-1': mockApiResponses.quoteItem.single,
      });

      const response = await fetch('/api/quote-items/test-id-1', {
        method: 'PATCH',
        body: JSON.stringify({ /* update data */ }),
      });

      expect(response.ok).toBe(true);
    });

    it('should delete QuoteItem', async () => {
      global.fetch = createMockFetch({
        '/api/quote-items/test-id-1': { success: true },
      });

      const response = await fetch('/api/quote-items/test-id-1', {
        method: 'DELETE',
      });

      expect(response.ok).toBe(true);
    });
  });

  describe('User CRUD', () => {
    it('should create User', async () => {
      global.fetch = createMockFetch({
        '/api/users': mockApiResponses.user.single,
      });

      // Simulate create operation
      const response = await fetch('/api/users', {
        method: 'POST',
        body: JSON.stringify(mockApiResponses.user.single),
      });

      expect(response.ok).toBe(true);
      const data = await response.json();
      expect(data.id).toBeDefined();
    });

    it('should read Users', async () => {
      global.fetch = createMockFetch({
        '/api/users': mockApiResponses.user.list,
      });

      const response = await fetch('/api/users');
      expect(response.ok).toBe(true);

      const data = await response.json();
      expect(Array.isArray(data)).toBe(true);
    });

    it('should update User', async () => {
      global.fetch = createMockFetch({
        '/api/users/test-id-1': mockApiResponses.user.single,
      });

      const response = await fetch('/api/users/test-id-1', {
        method: 'PATCH',
        body: JSON.stringify({ /* update data */ }),
      });

      expect(response.ok).toBe(true);
    });

    it('should delete User', async () => {
      global.fetch = createMockFetch({
        '/api/users/test-id-1': { success: true },
      });

      const response = await fetch('/api/users/test-id-1', {
        method: 'DELETE',
      });

      expect(response.ok).toBe(true);
    });
  });

  describe('Team CRUD', () => {
    it('should create Team', async () => {
      global.fetch = createMockFetch({
        '/api/teams': mockApiResponses.team.single,
      });

      // Simulate create operation
      const response = await fetch('/api/teams', {
        method: 'POST',
        body: JSON.stringify(mockApiResponses.team.single),
      });

      expect(response.ok).toBe(true);
      const data = await response.json();
      expect(data.id).toBeDefined();
    });

    it('should read Teams', async () => {
      global.fetch = createMockFetch({
        '/api/teams': mockApiResponses.team.list,
      });

      const response = await fetch('/api/teams');
      expect(response.ok).toBe(true);

      const data = await response.json();
      expect(Array.isArray(data)).toBe(true);
    });

    it('should update Team', async () => {
      global.fetch = createMockFetch({
        '/api/teams/test-id-1': mockApiResponses.team.single,
      });

      const response = await fetch('/api/teams/test-id-1', {
        method: 'PATCH',
        body: JSON.stringify({ /* update data */ }),
      });

      expect(response.ok).toBe(true);
    });

    it('should delete Team', async () => {
      global.fetch = createMockFetch({
        '/api/teams/test-id-1': { success: true },
      });

      const response = await fetch('/api/teams/test-id-1', {
        method: 'DELETE',
      });

      expect(response.ok).toBe(true);
    });
  });

});
