<mvc:View
  controllerName="hospital-management-system.controller.entity.BusAdmissionCreate"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns="sap.m"
  xmlns:f="sap.f"
  xmlns:form="sap.ui.layout.form"
  xmlns:core="sap.ui.core">

  <f:DynamicPage
    headerExpanded="false"
    toggleHeaderOnTitleClick="true"
    busy="{view>/busy}">

    <f:title>
      <f:DynamicPageTitle>
        <f:heading>
          <Title text="Create Bus Admission" />
        </f:heading>
        <f:actions>
          <Button
            text="Save"
            type="Emphasized"
            icon="sap-icon://save"
            press=".onSavePress" />
          <Button
            text="Cancel"
            press=".onCancelPress" />
        </f:actions>
        <f:navigationActions>
          <Button
            icon="sap-icon://nav-back"
            press=".onNavBack"
            tooltip="Back to List" />
        </f:navigationActions>
      </f:DynamicPageTitle>
    </f:title>

    <f:content>
      <VBox class="sapUiSmallMargin">
        <Panel headerText="General Information" expandable="true">
          <form:SimpleForm
            editable="true"
            layout="ResponsiveGridLayout"
            labelSpanXL="3"
            labelSpanL="3"
            labelSpanM="3"
            labelSpanS="12"
            adjustLabelSpan="false"
            emptySpanXL="4"
            emptySpanL="4"
            emptySpanM="4"
            emptySpanS="0"
            columnsXL="1"
            columnsL="1"
            columnsM="1">
            <Label text="Patient Id" required="true" />
            <Input id="patient_id" value="{view>/formData/patient_id}" />
            <Label text="Admission Date" required="true" />
            <Input id="admission_date" value="{view>/formData/admission_date}" />
            <Label text="Admission Time" required="true" />
            <Input id="admission_time" value="{view>/formData/admission_time}" />
            <Label text="Department Id" required="true" />
            <Input id="department_id" value="{view>/formData/department_id}" />
            <Label text="Ward Id" required="true" />
            <Input id="ward_id" value="{view>/formData/ward_id}" />
            <Label text="Room Id" required="true" />
            <Input id="room_id" value="{view>/formData/room_id}" />
            <Label text="Admission Type" required="true" />
            <Input id="admission_type" value="{view>/formData/admission_type}" />
            <Label text="Status" required="true" />
            <Input id="status" value="{view>/formData/status}" />
            <Label text="Priority" required="true" />
            <Input id="priority" value="{view>/formData/priority}" />
          </form:SimpleForm>
        </Panel>
      </VBox>
    </f:content>

  </f:DynamicPage>
</mvc:View>
